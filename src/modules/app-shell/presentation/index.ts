// Presentation-layer barrel for the app-shell module (PR 4b +
// PR 5b.4 promotion). Cross-module consumers MUST import from
// `@taxa/app-shell`.

export { AppShell } from "./AppShell";
export { BrowserSurface, type BrowserSurfaceProps } from "./BrowserSurface";
