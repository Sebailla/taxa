# Complete the frontend migration

## Objective
Finish issue #74 by replacing the legacy `web/` frontend with the React/Next static export served by FastAPI, while preserving behavior, a single local origin, and an atomic rollback.

## Authorization and boundaries
- User authorized beginning this migration plan on branch `feat/complete-frontend-migration`.
- This tracker starts with planning and read-only mapping; no production cutover, legacy deletion, commit, push, or PR creation is authorized yet.
- Preserve pre-existing worktree changes: `next-env.d.ts` and `odd/tasks/g4-integration-delivery.md`.
- The final Approach selection remains evidence-gated. Static export under FastAPI (Approach A) is the default only if its gates pass.

## Delivery strategy
Feature Branch Chain. Each code-bearing slice keeps its tests and documentation. The final cutover is a single atomic release even if it exceeds the usual review budget, because partial activation would break active consumers and rollback.

## Tasks
- [x] ODD-MIGRATE-001 Map the remaining legacy research surface into exact React replacement slices.
  - Route: delegated explorer (four-file rule).
  - Scope: file explorer, viewers/renderers, materialization, keyboard behavior, and AC-21 consumers.
  - Evidence: the read-only map identifies 18 dependency-ordered units (W1–W18). W1–W6 establish pure Research domain/ports/API/search/renderers; W7–W12 mount explorer/viewer/folder/keymap/settings/detail UI; W13–W15 relocate AC-21/search/CSS; W16–W17 integrate the route; W18 is the separately authorized atomic cutover. The map forecasts 16 reviewable child slices plus one atomic release, and preserves Tier-1 consumers until cutover. Key boundaries: raw `taxa.fex.treeWidth` must stay local to Explorer to avoid browser-state chunk scope expansion; React materialize propagation must update typed tree state rather than legacy DOM markers; `next-env.d.ts` remains untouched.

- [ ] ODD-MIGRATE-002 Define failing React contracts for the research replacement.
  - Route: delegated worker after the map is approved.
  - Scope: domain/application ports, API contract seams, and hermetic tests only.
  - Progress: W1 Research domain contract is implemented and independently verified: `src/modules/research/domain/explorer.ts` defines pure, readonly ExplorerState/SearchState/ViewerTab/FileFormat and the legacy-faithful initial-state factory; `tests/test_research_domain.py` passes 20 tests. Independent verification also passed 170 relevant purity/layer tests and strict isolated TypeScript compilation. Work-unit commit: `2699e8f feat(research): add explorer domain contract`. The user explicitly accepted its documented size exception: 1,130 added lines keep the domain contract, its runtime proof, and the feature tracker cohesive; splitting would separate behavior from proof. W2 is also implemented and independently verified: `src/modules/research/application/ports.ts` adds the pure inward-only ExplorerRepository contract for tree retrieval and file serving; `tests/test_research_application.py` passes 16 focused tests, including strict compilation and a runtime port-compatibility harness. The user explicitly accepted W2's documented size exception (~1,091 lines) because the pure port and its runtime proof must remain together. W3 Research infrastructure adapter is implemented by this work unit: `src/modules/research/infrastructure/api.ts` ships the typed FastAPI adapter satisfying `ExplorerRepository` structurally via TypeScript function subtyping — the wider signatures with transport-level `fetch` + `baseUrl` options are silently dropped on the narrower port signature, the same way the merged taxonomy adapter satisfies `TaxonomyRepository`. Exports `fetchFiles()` (GET /api/files → ExplorerTree) and `fetchFileServe(request)` (GET /api/files/serve → typed bytes + Content-Type + filename), both with optional injected fetch and baseUrl options. Defines named `ExplorerApiError` (mirrors `TaxonomyApiError` shape — `{ status, cause }`); validated wire projection that surfaces every ExplorerTree/ExplorerFolderNode/ExplorerFileNode field verbatim with no client-side coercion; encoded server path query (`encodeURIComponent`, mirrors `web/file_explorer.js::serveUrl`); typed file bytes (`Uint8Array` from `response.arrayBuffer()`); verbatim Content-Type (`response.headers.get("content-type")`); parsed filename (Starlette's `Content-Disposition: inline; filename="<basename>"` with RFC 5987 `filename*=UTF-8''...` fallback for non-ASCII basenames); and clear HTTP / malformed-response errors (status in message + on `ExplorerApiError.status`). The public barrel `src/modules/research/index.ts` re-exports `fetchFiles`, `fetchFileServe`, `ExplorerApiError`, and the `FetchOptions` interface so cross-module consumers reach the adapter through the barrel (spec.md rule 5). `tests/test_research_infra.py` adds focused hermetic pytest contract tests with injected fetch + strict isolated TypeScript/Node harness, mirroring the `tests/test_taxonomy_infra.py` pattern: file presence + source-level purity (no React/Next/FastAPI/framework tokens; inward deps only into `../domain/explorer` and `../application/ports`), source-level export contracts (named `fetchFiles`, `fetchFileServe`, `ExplorerApiError`, `FetchOptions`), isolated strict compile under `--lib ES2022` only, runtime harness with stubbed `fetch` covering the tested error branches (HTTP non-OK, malformed JSON, non-object tree payload, unknown node type, missing file fields, missing tree fields) + every wire projection path (folder/file tree with nested children, `exists: false` empty-state path, encoded path query with spaces/accents, Content-Type/Content-Disposition extraction, RFC 5987 filename fallback, baseUrl joining, default fetch missing-error guard) + port-compat runtime mirror (the compiled adapter must be wire-compatible with the W2 port at runtime). No FastAPI mount change. `next-env.d.ts` and `odd/tasks/g4-integration-delivery.md` remain untouched (preserved pre-existing dirty files).
  - Evidence (this work unit): the implementation lands + the hermetic test harness passes under the focused pytest run; RED → GREEN observed for the file-presence guard + the source-level purity guard + the export-contract guards; the strict isolated TypeScript compile passes (no DOM, no framework types); the runtime harness passes all 25+ assertions. Independent verification passed: 15 W3 tests, the 51-test W1–W3 regression group, the 74-test Research/Taxonomy cross-module group, and project-wide strict TypeScript compilation are green. The user explicitly accepted W3's documented size exception (~1,900 lines): the adapter, its runtime proof, public barrel, and tracker evidence are one cohesive contract. Work-unit commit: `f2da8ab feat(research): add explorer infrastructure adapter`. The ODD-MIGRATE-002 task bullet stays open because W4+ contracts remain. The user selected a split W4 plan: W4a will cover framework-free, no-CDN preview dispatch and preserve legacy Markdown-as-text behavior; CDN-dependent preview families and any Markdown-as-HTML change are deferred to separate reviewable slices.
  - Progress: W4a viewer-dispatch contract is implemented by this work unit: `src/modules/research/application/renderers.ts` ships the pure application-layer viewer-dispatch contract covering the eight no-CDN families the W4a slice owns — PDF (`pdf-iframe` + legacy download fallback), HTML/HTM (`html-iframe` with `sandbox: ""` — NO `allow-same-origin`), TXT/MD (`text-pre` with UTF-8 decoded body — W4a PRESERVES legacy Markdown-as-text behavior verbatim), DOC (`unsupported` with the spec's exact `"Legacy .doc cannot be rendered inline."` wording), JPG/JPEG/PNG/GIF/WEBP/BMP (`image` with the legacy 50 MB advisory), SVG (`svg-sanitized` via the pure string-level XSS scrub — strips `<script>` blocks AND `on*=` event-handler attrs without `DOMParser` or `document.createTreeWalker`), MP4/WEBM/OGV (`video` with `controls: true` + `preload: "metadata"`), `"other"` extension fallback (`unsupported` with the wire-extension message), and the Table/Tree `tab-not-applicable` feedback (legacy `${tab} view not available for .${ext} files — use Raw.` wording pinned byte-for-byte via the exported `TAB_NOT_APPLICABLE_SUFFIX` constant). Defines `dispatchViewer(input: ViewerDispatchInput): ViewerDispatch` (pure dispatcher — input → typed `ViewerDispatch` outcome, NO `await`, NO I/O), `sanitizeSvgMarkup(svgText: string): string` (pure string-level XSS scrub), `IMAGE_BIG_FILE_BYTES = 50 * 1024 * 1024` (legacy threshold pinned byte-for-byte), and the typed `ViewerFileDescriptor` / `ViewerDispatchInput` / `ViewerDispatch` / `ViewerLink` / `ViewerImageAdvisory` surface. URLs are derived from the explicit `ViewerFileDescriptor.url` input field — the dispatcher does NOT import the W3 `fetchFileServe` infrastructure adapter (the W4a split directive), preserving the layered architecture (spec.md rule 4 — application depends on domain ONLY). Bytes are injected through the typed `bytes: Uint8Array | null` input (the W2 port's byte shape) — no `fetch(`, no DOM access. CDN-dependent families (DOCX via mammoth, XLS/XLSX via SheetJS, EPUB via epubjs, CSV/TSV via Papa Parse, JSON) and Markdown-as-HTML are explicitly deferred to W4b+ — the dispatcher returns the `unsupported` branch (with the format-literal `"Format .{ext} not supported in viewer."` message) or the `tab-not-applicable` branch for those combinations until the later slices extend the contract. The public barrel `src/modules/research/index.ts` re-exports `dispatchViewer`, `sanitizeSvgMarkup`, `IMAGE_BIG_FILE_BYTES`, `TAB_NOT_APPLICABLE_SUFFIX` (as values) and `ViewerDispatch`, `ViewerDispatchInput`, `ViewerFileDescriptor`, `ViewerLink`, `ViewerImageAdvisory` (as types) so cross-module consumers reach the W4a contract through the barrel (spec.md rule 5). `tests/test_research_renderers.py` adds 13 focused hermetic pytest contract tests: file presence + plain-TS + source-level purity (no React/Next/FastAPI/Starlette/fetch/`DOMParser`/document/window/localStorage/process/require/globalThis/Buffer.atob/btoa/Blob/FileReader imports; inward deps only into `../domain/explorer`), no-default-export guard, named-export contracts for `dispatchViewer` / `sanitizeSvgMarkup` / `IMAGE_BIG_FILE_BYTES` / `TAB_NOT_APPLICABLE_SUFFIX`, barrel re-export contract for all four values + five types, isolated strict compile under `--lib ES2022` only, runtime harness with strict-equality assertions covering every W4a-supported format dispatch + every W4a-deferred format dispatch + the SVG sanitizer's every branch (valid document, `<script>` removal, `on*=` removal, multi-block/multi-attribute stress, document-invalid fallback, non-event attribute preservation) + the legacy message wording pinned byte-for-byte + the bytes-independence contract + the dispatcher purity contract + the W1 ↔ W4a domain dependency round-trip + the W4a-deferred formats on Table/Tree tabs also fire `tab-not-applicable`. No W3 changes (the adapter stays untouched per the W4a split). No FastAPI changes. No legacy edits. No browser-state changes. No CSS. No CDN seams. No commits, pushes, or PRs.
  - Evidence (this work unit): the implementation lands + the hermetic test harness passes under the focused pytest run; 13/13 focused W4a tests green; the 64-test W1–W4a regression group (20 W1 domain + 16 W2 application + 15 W3 infra + 13 W4a renderers) green; the 129-test cross-module group (Research + Taxonomy + module layers + restricted imports) green; the 231-test extended verification group (Research + Taxonomy + module layers + restricted imports) green; project-wide strict TypeScript compilation across `src/modules/research/` green. Independent verification passed: 13 focused W4a tests cover the final SVG sanitizer correction (paired and self-closing script elements plus mixed-case event attributes); the W1–W4a regression group, cross-module/layer guards, and strict TypeScript compilation remain green. The user explicitly accepted W4a's documented size exception (~2,000 lines): its pure contract, runtime proof, barrel, and tracker evidence are cohesive. Work-unit commit: `46c0eff feat(research): add viewer dispatcher contract`. The ODD-MIGRATE-002 task bullet stays open because W4b+ contracts remain. The user selected four reviewable W4b slices: W4b1 DOCX, W4b2 XLS/XLSX, W4b3 EPUB, and W4b4 CSV/TSV plus JSON; each preserves the pure dispatcher and defers CDN script loading and DOM lifecycle to the later React mount.
  - Progress: W4b1 viewer-dispatch contract is implemented by this work unit: `src/modules/research/application/renderers.ts` extends the pure application-layer viewer-dispatch contract with two new `ViewerDispatch` variants (`docx-source` and `docx-offline`) carrying the typed source descriptor + bytes + pinned mammoth CDN URL + window-global name so a future React mount (W6+) can load the legacy-pinned mammoth library via Next 16's `<Script src={scriptUrl} strategy="afterInteractive" onLoad={convert} onError={…}>` (see `node_modules/next/dist/docs/01-app/03-api-reference/02-components/script.md`) and call `window[scriptGlobal].convertToHtml({arrayBuffer: bytes.buffer})`. The application layer stays framework-free, browser-free, and CDN-loader-free — mammoth is NOT imported or loaded here; the dispatcher only emits the typed source descriptor for the mount to consume. The W4b1 contract adds:
    - `MAMMOTH_CDN_URL: string` — pinned CDN URL (`https://cdn.jsdelivr.net/npm/mammoth@1.8.0/mammoth.browser.min.js`) mirroring `web/file_viewer.js::CDN_URLS.mammoth` + `web/index.html`'s matching `<script>` tag (whose inline comment calls the URL "Pinned URL: do not unpin."). Pinned byte-for-byte; bumping requires updating this constant AND the `<script>` tag AND the focused test that pins the literal.
    - `MAMMOTH_GLOBAL_NAME: string` — the literal `"mammoth"` global name mammoth assigns itself once the CDN loads. Mirrors the legacy `window.mammoth.convertToHtml` call site + the `CDN_URLS.mammoth` map key.
    - `"docx-source"` `ViewerDispatch` variant — typed source descriptor carrying `src` (descriptor URL verbatim), `title` (descriptor name), `bytes` (the SAME `Uint8Array` reference as the input — pass-by-reference contract, the future mount reads bytes at mount time, no copy), `scriptUrl` (`MAMMOTH_CDN_URL`), `scriptGlobal` (`MAMMOTH_GLOBAL_NAME`). Emitted when `format === "docx"` + `tab === "Raw"` + `bytes !== null`.
    - `"docx-offline"` `ViewerDispatch` variant — typed offline fallback carrying `name` (descriptor name), `download: ViewerLink` (the W4a pattern), `scriptUrl`, `scriptGlobal`, `reason: "bytes-missing"` (typed literal — the only offline path the dispatcher can detect at dispatch time, since CDN-load + convertToHtml failures happen at the mount). Emitted when `format === "docx"` + `tab === "Raw"` + `bytes === null`. Mirrors the legacy `web/file_viewer.js::renderOfflineBanner` recovery path so the future mount paints the same "Viewer offline — raw download available for X" banner verbatim. Does NOT carry a free-form `message` string — the mount owns the wording.
    - `case "docx":` switch arm — added BEFORE the `case "other":` and `default:` arms. The format switch now dispatches DOCX explicitly; XLS/XLSX/EPUB/CSV/TSV/JSON remain in the default arm until W4b2–W4b4 land.
    - `buildDocxOffline(file)` helper — constructs the `docx-offline` branch with `reason: "bytes-missing"`.
    The Table/Tree gate still short-circuits before the format switch, so DOCX on Table/Tree still hits the `tab-not-applicable` branch (DOCX has no Table/Tree renderer — the W4b1 split is Raw-only via mammoth).
  - The public barrel `src/modules/research/index.ts` re-exports `MAMMOTH_CDN_URL` and `MAMMOTH_GLOBAL_NAME` (as values) so cross-module consumers reach the W4b1 CDN pin through the barrel (spec.md rule 5). The two new variants live inside the existing `ViewerDispatch` discriminated union (no new top-level types) — the W4b2–W4b4 slices follow the same pattern.
  - `tests/test_research_renderers.py` extends the W4a test surface with 3 new focused tests: `test_renderers_file_has_no_docx_renderer_or_loader_imports` (pins the no-mammoth-import, no-`loadScriptOnce`, no-`<script>`-injection, no-`convertToHtml` purity contract — the dispatcher only emits the typed source descriptor), `test_renderers_file_exports_named_mammoth_cdn_url` (pins the `MAMMOTH_CDN_URL` export + the legacy-pinned URL literal), `test_renderers_file_exports_named_mammoth_global_name` (pins the `MAMMOTH_GLOBAL_NAME` export + the `"mammoth"` literal). The barrel re-export test is extended to include the two new constants. The runtime harness removes DOCX from the W4a-deferred Raw-tab loop (step 20) and adds 8 new steps (30–37) that exercise: DOCX + Raw + bytes → `docx-source` with descriptor + bytes + pinned URL + global name; DOCX + Raw + bytes=null → `docx-offline` with download + pinned URL + global name + `reason: "bytes-missing"`; DOCX on Table/Tree → `tab-not-applicable` (Table/Tree gate preserved); DOCX dispatcher purity on both source + offline branches; DOCX bytes-reference contract (the dispatched `bytes` is the SAME `Uint8Array` reference as the input — pass-by-reference, the future mount treats the bytes as read-only); bytes mutation visible through the dispatched reference (the dispatcher does NOT copy). The Markdown-as-text behavior is preserved (step 7 still asserts MD → text-pre with UTF-8 body verbatim). The W4a SVG sanitizer's every branch (paired + self-closing `<script>` + case-insensitive `on*=` attrs) stays intact.
  - Evidence (this work unit): the implementation lands + the hermetic test harness passes under the focused pytest run; 16/16 focused W4a + W4b1 tests green (13 W4a + 3 W4b1); the 67-test W1–W4b1 regression group (20 W1 domain + 16 W2 application + 15 W3 infra + 16 W4a + W4b1 renderers) green; the 290-test cross-module/layer/regression group (Research + Taxonomy + module layers + restricted imports) green; project-wide strict TypeScript compilation across `src/modules/research/` green; the strict isolated compile under `--lib ES2022` only (no DOM, no framework types) passes. Independent verification passed: 16 focused tests, the 67-test W1–W4b1 regression group, and the 290-test cross-module/layer group are green. The user explicitly accepted W4b1's documented size exception (~722 added lines): its application contract, runtime proof, barrel, and tracker evidence are cohesive. Work-unit commit: pending. The ODD-MIGRATE-002 task bullet stays open because W4b2–W4b4 contracts remain.

- [ ] ODD-MIGRATE-003 Deliver the Research explorer and supported file-preview behavior in reviewable slices.
  - Route: delegated worker; chained PRs expected.
  - Scope: `src/modules/research/` and focused tests, preserving legacy behavior.
  - Evidence: focused contracts and browser verification for explorer, materialization, open-folder, and each supported preview family.

- [ ] ODD-MIGRATE-004 Move AC-21 and remaining active consumers off `web/`.
  - Route: delegated worker.
  - Scope: search-engine catalog, consumer manifest, and affected tests.
  - Evidence: AC-21, smoke, and consumer-manifest tests pass against the React replacement path.

- [ ] ODD-MIGRATE-005 Produce fresh candidate evidence and select the cutover approach.
  - Route: delegated verification.
  - Scope: reproducible build inventory (G2), behavior/a11y parity (G4), candidate-baseline comparison (G5), and canonical dry-run rehearsal (G6).
  - Evidence: all artifacts are current, comparable, and pass their fail-closed thresholds. If any gate fails, stop before activation.

- [ ] ODD-MIGRATE-006 Perform the atomic production cutover.
  - Route: delegated worker after ODD-MIGRATE-005 passes and the user explicitly authorizes activation.
  - Scope: `make api` build path, `api/server.py::WEB_DIR`, all Tier-2 consumer updates, the selected build artifact, and legacy `web/` retirement in one release.
  - Evidence: a single rollback boundary and no active consumer pointed at a retired path.

- [ ] ODD-MIGRATE-007 Verify the activated runtime and rollback.
  - Route: delegated verifier.
  - Scope: full relevant tests, FastAPI-served smoke/browser/a11y validation, G3 Tier-2 readiness, G6 execution/rollback rehearsal, and a revert proof.
  - Evidence: CI-equivalent suite and the rollback restore the legacy behavior as one unit.

- [ ] ODD-MIGRATE-008 Close issue #74.
  - Route: parent after all evidence and explicit user authorization.
  - Evidence: every acceptance criterion is met, CI is green, and the issue closure comment links the cutover and verification evidence.

## Current evidence
- The React taxonomy surface, detail panels, browser-state store, static-export preparation, and G4/G6 supporting slices are merged into `develop`.
- FastAPI still serves `web/`; this branch has not changed that behavior.
- `src/modules/research/` remains the main functional gap.
- G6 canonical dry-run is recorded, but G3 Tier-2, G4 candidate parity, G5 candidate comparison, activation, and rollback rehearsal remain incomplete.

## Work-unit policy
No work-unit commit is made unless the user explicitly authorizes commits. Once authorized, each completed task must include its tests/docs and use a Conventional Commit message; the final atomic cutover is a separately authorized release boundary.
