# Complete the frontend migration

## Objective
Finish issue #74 by replacing the legacy `web/` frontend with the React/Next static export served by FastAPI, while preserving behavior, a single local origin, and an atomic rollback.

## Authorization and boundaries
- User authorized beginning this migration plan on branch `feat/complete-frontend-migration`.
- This tracker starts with planning and read-only mapping; no production cutover, legacy deletion, commit, push, or PR creation is authorized yet.
- Preserve pre-existing worktree changes: `next-env.d.ts` and `odd/tasks/g4-integration-delivery.md`.
- The final Approach selection remains evidence-gated. Static export under FastAPI (Approach A) is the default only if its gates pass.

## Delivery strategy
Feature Branch Chain. Each code-bearing slice keeps its tests and documentation. The final cutover is a single atomic release even if it exceeds the usual review budget, because partial activation would break active consumers and rollback.

## Tasks
- [x] ODD-MIGRATE-001 Map the remaining legacy research surface into exact React replacement slices.
  - Route: delegated explorer (four-file rule).
  - Scope: file explorer, viewers/renderers, materialization, keyboard behavior, and AC-21 consumers.
  - Evidence: the read-only map identifies 18 dependency-ordered units (W1–W18). W1–W6 establish pure Research domain/ports/API/search/renderers; W7–W12 mount explorer/viewer/folder/keymap/settings/detail UI; W13–W15 relocate AC-21/search/CSS; W16–W17 integrate the route; W18 is the separately authorized atomic cutover. The map forecasts 16 reviewable child slices plus one atomic release, and preserves Tier-1 consumers until cutover. Key boundaries: raw `taxa.fex.treeWidth` must stay local to Explorer to avoid browser-state chunk scope expansion; React materialize propagation must update typed tree state rather than legacy DOM markers; `next-env.d.ts` remains untouched.

- [ ] ODD-MIGRATE-002 Define failing React contracts for the research replacement.
  - Route: delegated worker after the map is approved.
  - Scope: domain/application ports, API contract seams, and hermetic tests only.
  - Progress: W1 Research domain contract is implemented and independently verified: `src/modules/research/domain/explorer.ts` defines pure, readonly ExplorerState/SearchState/ViewerTab/FileFormat and the legacy-faithful initial-state factory; `tests/test_research_domain.py` passes 20 tests. Independent verification also passed 170 relevant purity/layer tests and strict isolated TypeScript compilation. Work-unit commit: `2699e8f feat(research): add explorer domain contract`. The user explicitly accepted its documented size exception: 1,130 added lines keep the domain contract, its runtime proof, and the feature tracker cohesive; splitting would separate behavior from proof. W2 is also implemented and independently verified: `src/modules/research/application/ports.ts` adds the pure inward-only ExplorerRepository contract for tree retrieval and file serving; `tests/test_research_application.py` passes 16 focused tests, including strict compilation and a runtime port-compatibility harness. The user explicitly accepted W2's documented size exception (~1,091 lines) because the pure port and its runtime proof must remain together. W3 Research infrastructure adapter is implemented by this work unit: `src/modules/research/infrastructure/api.ts` ships the typed FastAPI adapter satisfying `ExplorerRepository` structurally via TypeScript function subtyping — the wider signatures with transport-level `fetch` + `baseUrl` options are silently dropped on the narrower port signature, the same way the merged taxonomy adapter satisfies `TaxonomyRepository`. Exports `fetchFiles()` (GET /api/files → ExplorerTree) and `fetchFileServe(request)` (GET /api/files/serve → typed bytes + Content-Type + filename), both with optional injected fetch and baseUrl options. Defines named `ExplorerApiError` (mirrors `TaxonomyApiError` shape — `{ status, cause }`); validated wire projection that surfaces every ExplorerTree/ExplorerFolderNode/ExplorerFileNode field verbatim with no client-side coercion; encoded server path query (`encodeURIComponent`, mirrors `web/file_explorer.js::serveUrl`); typed file bytes (`Uint8Array` from `response.arrayBuffer()`); verbatim Content-Type (`response.headers.get("content-type")`); parsed filename (Starlette's `Content-Disposition: inline; filename="<basename>"` with RFC 5987 `filename*=UTF-8''...` fallback for non-ASCII basenames); and clear HTTP / malformed-response errors (status in message + on `ExplorerApiError.status`). The public barrel `src/modules/research/index.ts` re-exports `fetchFiles`, `fetchFileServe`, `ExplorerApiError`, and the `FetchOptions` interface so cross-module consumers reach the adapter through the barrel (spec.md rule 5). `tests/test_research_infra.py` adds focused hermetic pytest contract tests with injected fetch + strict isolated TypeScript/Node harness, mirroring the `tests/test_taxonomy_infra.py` pattern: file presence + source-level purity (no React/Next/FastAPI/framework tokens; inward deps only into `../domain/explorer` and `../application/ports`), source-level export contracts (named `fetchFiles`, `fetchFileServe`, `ExplorerApiError`, `FetchOptions`), isolated strict compile under `--lib ES2022` only, runtime harness with stubbed `fetch` covering the tested error branches (HTTP non-OK, malformed JSON, non-object tree payload, unknown node type, missing file fields, missing tree fields) + every wire projection path (folder/file tree with nested children, `exists: false` empty-state path, encoded path query with spaces/accents, Content-Type/Content-Disposition extraction, RFC 5987 filename fallback, baseUrl joining, default fetch missing-error guard) + port-compat runtime mirror (the compiled adapter must be wire-compatible with the W2 port at runtime). No FastAPI mount change. `next-env.d.ts` and `odd/tasks/g4-integration-delivery.md` remain untouched (preserved pre-existing dirty files).
  - Evidence (this work unit): the implementation lands + the hermetic test harness passes under the focused pytest run; RED → GREEN observed for the file-presence guard + the source-level purity guard + the export-contract guards; the strict isolated TypeScript compile passes (no DOM, no framework types); the runtime harness passes all 25+ assertions. Independent verification passed: 15 W3 tests, the 51-test W1–W3 regression group, the 74-test Research/Taxonomy cross-module group, and project-wide strict TypeScript compilation are green. The user explicitly accepted W3's documented size exception (~1,900 lines): the adapter, its runtime proof, public barrel, and tracker evidence are one cohesive contract. Work-unit commit: `f2da8ab feat(research): add explorer infrastructure adapter`. The ODD-MIGRATE-002 task bullet stays open because W4+ contracts remain. The user selected a split W4 plan: W4a will cover framework-free, no-CDN preview dispatch and preserve legacy Markdown-as-text behavior; CDN-dependent preview families and any Markdown-as-HTML change are deferred to separate reviewable slices.
  - Progress: W4a viewer-dispatch contract is implemented by this work unit: `src/modules/research/application/renderers.ts` ships the pure application-layer viewer-dispatch contract covering the eight no-CDN families the W4a slice owns — PDF (`pdf-iframe` + legacy download fallback), HTML/HTM (`html-iframe` with `sandbox: ""` — NO `allow-same-origin`), TXT/MD (`text-pre` with UTF-8 decoded body — W4a PRESERVES legacy Markdown-as-text behavior verbatim), DOC (`unsupported` with the spec's exact `"Legacy .doc cannot be rendered inline."` wording), JPG/JPEG/PNG/GIF/WEBP/BMP (`image` with the legacy 50 MB advisory), SVG (`svg-sanitized` via the pure string-level XSS scrub — strips `<script>` blocks AND `on*=` event-handler attrs without `DOMParser` or `document.createTreeWalker`), MP4/WEBM/OGV (`video` with `controls: true` + `preload: "metadata"`), `"other"` extension fallback (`unsupported` with the wire-extension message), and the Table/Tree `tab-not-applicable` feedback (legacy `${tab} view not available for .${ext} files — use Raw.` wording pinned byte-for-byte via the exported `TAB_NOT_APPLICABLE_SUFFIX` constant). Defines `dispatchViewer(input: ViewerDispatchInput): ViewerDispatch` (pure dispatcher — input → typed `ViewerDispatch` outcome, NO `await`, NO I/O), `sanitizeSvgMarkup(svgText: string): string` (pure string-level XSS scrub), `IMAGE_BIG_FILE_BYTES = 50 * 1024 * 1024` (legacy threshold pinned byte-for-byte), and the typed `ViewerFileDescriptor` / `ViewerDispatchInput` / `ViewerDispatch` / `ViewerLink` / `ViewerImageAdvisory` surface. URLs are derived from the explicit `ViewerFileDescriptor.url` input field — the dispatcher does NOT import the W3 `fetchFileServe` infrastructure adapter (the W4a split directive), preserving the layered architecture (spec.md rule 4 — application depends on domain ONLY). Bytes are injected through the typed `bytes: Uint8Array | null` input (the W2 port's byte shape) — no `fetch(`, no DOM access. CDN-dependent families (DOCX via mammoth, XLS/XLSX via SheetJS, EPUB via epubjs, CSV/TSV via Papa Parse, JSON) and Markdown-as-HTML are explicitly deferred to W4b+ — the dispatcher returns the `unsupported` branch (with the format-literal `"Format .{ext} not supported in viewer."` message) or the `tab-not-applicable` branch for those combinations until the later slices extend the contract. The public barrel `src/modules/research/index.ts` re-exports `dispatchViewer`, `sanitizeSvgMarkup`, `IMAGE_BIG_FILE_BYTES`, `TAB_NOT_APPLICABLE_SUFFIX` (as values) and `ViewerDispatch`, `ViewerDispatchInput`, `ViewerFileDescriptor`, `ViewerLink`, `ViewerImageAdvisory` (as types) so cross-module consumers reach the W4a contract through the barrel (spec.md rule 5). `tests/test_research_renderers.py` adds 13 focused hermetic pytest contract tests: file presence + plain-TS + source-level purity (no React/Next/FastAPI/Starlette/fetch/`DOMParser`/document/window/localStorage/process/require/globalThis/Buffer.atob/btoa/Blob/FileReader imports; inward deps only into `../domain/explorer`), no-default-export guard, named-export contracts for `dispatchViewer` / `sanitizeSvgMarkup` / `IMAGE_BIG_FILE_BYTES` / `TAB_NOT_APPLICABLE_SUFFIX`, barrel re-export contract for all four values + five types, isolated strict compile under `--lib ES2022` only, runtime harness with strict-equality assertions covering every W4a-supported format dispatch + every W4a-deferred format dispatch + the SVG sanitizer's every branch (valid document, `<script>` removal, `on*=` removal, multi-block/multi-attribute stress, document-invalid fallback, non-event attribute preservation) + the legacy message wording pinned byte-for-byte + the bytes-independence contract + the dispatcher purity contract + the W1 ↔ W4a domain dependency round-trip + the W4a-deferred formats on Table/Tree tabs also fire `tab-not-applicable`. No W3 changes (the adapter stays untouched per the W4a split). No FastAPI changes. No legacy edits. No browser-state changes. No CSS. No CDN seams. No commits, pushes, or PRs.
  - Evidence (this work unit): the implementation lands + the hermetic test harness passes under the focused pytest run; 13/13 focused W4a tests green; the 64-test W1–W4a regression group (20 W1 domain + 16 W2 application + 15 W3 infra + 13 W4a renderers) green; the 129-test cross-module group (Research + Taxonomy + module layers + restricted imports) green; the 231-test extended verification group (Research + Taxonomy + module layers + restricted imports) green; project-wide strict TypeScript compilation across `src/modules/research/` green. Independent verification passed: 13 focused W4a tests cover the final SVG sanitizer correction (paired and self-closing script elements plus mixed-case event attributes); the W1–W4a regression group, cross-module/layer guards, and strict TypeScript compilation remain green. The user explicitly accepted W4a's documented size exception (~2,000 lines): its pure contract, runtime proof, barrel, and tracker evidence are cohesive. Work-unit commit: `46c0eff feat(research): add viewer dispatcher contract`. The ODD-MIGRATE-002 task bullet stays open because W4b+ contracts remain. The user selected four reviewable W4b slices: W4b1 DOCX, W4b2 XLS/XLSX, W4b3 EPUB, and W4b4 CSV/TSV plus JSON; each preserves the pure dispatcher and defers CDN script loading and DOM lifecycle to the later React mount.
  - Progress: W4b1 viewer-dispatch contract is implemented by this work unit: `src/modules/research/application/renderers.ts` extends the pure application-layer viewer-dispatch contract with two new `ViewerDispatch` variants (`docx-source` and `docx-offline`) carrying the typed source descriptor + bytes + pinned mammoth CDN URL + window-global name so a future React mount (W6+) can load the legacy-pinned mammoth library via Next 16's `<Script src={scriptUrl} strategy="afterInteractive" onLoad={convert} onError={…}>` (see `node_modules/next/dist/docs/01-app/03-api-reference/02-components/script.md`) and call `window[scriptGlobal].convertToHtml({arrayBuffer: bytes.buffer})`. The application layer stays framework-free, browser-free, and CDN-loader-free — mammoth is NOT imported or loaded here; the dispatcher only emits the typed source descriptor for the mount to consume. The W4b1 contract adds:
    - `MAMMOTH_CDN_URL: string` — pinned CDN URL (`https://cdn.jsdelivr.net/npm/mammoth@1.8.0/mammoth.browser.min.js`) mirroring `web/file_viewer.js::CDN_URLS.mammoth` + `web/index.html`'s matching `<script>` tag (whose inline comment calls the URL "Pinned URL: do not unpin."). Pinned byte-for-byte; bumping requires updating this constant AND the `<script>` tag AND the focused test that pins the literal.
    - `MAMMOTH_GLOBAL_NAME: string` — the literal `"mammoth"` global name mammoth assigns itself once the CDN loads. Mirrors the legacy `window.mammoth.convertToHtml` call site + the `CDN_URLS.mammoth` map key.
    - `"docx-source"` `ViewerDispatch` variant — typed source descriptor carrying `src` (descriptor URL verbatim), `title` (descriptor name), `bytes` (the SAME `Uint8Array` reference as the input — pass-by-reference contract, the future mount reads bytes at mount time, no copy), `scriptUrl` (`MAMMOTH_CDN_URL`), `scriptGlobal` (`MAMMOTH_GLOBAL_NAME`). Emitted when `format === "docx"` + `tab === "Raw"` + `bytes !== null`.
    - `"docx-offline"` `ViewerDispatch` variant — typed offline fallback carrying `name` (descriptor name), `download: ViewerLink` (the W4a pattern), `scriptUrl`, `scriptGlobal`, `reason: "bytes-missing"` (typed literal — the only offline path the dispatcher can detect at dispatch time, since CDN-load + convertToHtml failures happen at the mount). Emitted when `format === "docx"` + `tab === "Raw"` + `bytes === null`. Mirrors the legacy `web/file_viewer.js::renderOfflineBanner` recovery path so the future mount paints the same "Viewer offline — raw download available for X" banner verbatim. Does NOT carry a free-form `message` string — the mount owns the wording.
    - `case "docx":` switch arm — added BEFORE the `case "other":` and `default:` arms. The format switch now dispatches DOCX explicitly; XLS/XLSX/EPUB/CSV/TSV/JSON remain in the default arm until W4b2–W4b4 land.
    - `buildDocxOffline(file)` helper — constructs the `docx-offline` branch with `reason: "bytes-missing"`.
    The Table/Tree gate still short-circuits before the format switch, so DOCX on Table/Tree still hits the `tab-not-applicable` branch (DOCX has no Table/Tree renderer — the W4b1 split is Raw-only via mammoth).
  - The public barrel `src/modules/research/index.ts` re-exports `MAMMOTH_CDN_URL` and `MAMMOTH_GLOBAL_NAME` (as values) so cross-module consumers reach the W4b1 CDN pin through the barrel (spec.md rule 5). The two new variants live inside the existing `ViewerDispatch` discriminated union (no new top-level types) — the W4b2–W4b4 slices follow the same pattern.
  - `tests/test_research_renderers.py` extends the W4a test surface with 3 new focused tests: `test_renderers_file_has_no_docx_renderer_or_loader_imports` (pins the no-mammoth-import, no-`loadScriptOnce`, no-`<script>`-injection, no-`convertToHtml` purity contract — the dispatcher only emits the typed source descriptor), `test_renderers_file_exports_named_mammoth_cdn_url` (pins the `MAMMOTH_CDN_URL` export + the legacy-pinned URL literal), `test_renderers_file_exports_named_mammoth_global_name` (pins the `MAMMOTH_GLOBAL_NAME` export + the `"mammoth"` literal). The barrel re-export test is extended to include the two new constants. The runtime harness removes DOCX from the W4a-deferred Raw-tab loop (step 20) and adds 8 new steps (30–37) that exercise: DOCX + Raw + bytes → `docx-source` with descriptor + bytes + pinned URL + global name; DOCX + Raw + bytes=null → `docx-offline` with download + pinned URL + global name + `reason: "bytes-missing"`; DOCX on Table/Tree → `tab-not-applicable` (Table/Tree gate preserved); DOCX dispatcher purity on both source + offline branches; DOCX bytes-reference contract (the dispatched `bytes` is the SAME `Uint8Array` reference as the input — pass-by-reference, the future mount treats the bytes as read-only); bytes mutation visible through the dispatched reference (the dispatcher does NOT copy). The Markdown-as-text behavior is preserved (step 7 still asserts MD → text-pre with UTF-8 body verbatim). The W4a SVG sanitizer's every branch (paired + self-closing `<script>` + case-insensitive `on*=` attrs) stays intact.
  - Evidence (this work unit): the implementation lands + the hermetic test harness passes under the focused pytest run; 16/16 focused W4a + W4b1 tests green (13 W4a + 3 W4b1); the 67-test W1–W4b1 regression group (20 W1 domain + 16 W2 application + 15 W3 infra + 16 W4a + W4b1 renderers) green; the 290-test cross-module/layer/regression group (Research + Taxonomy + module layers + restricted imports) green; project-wide strict TypeScript compilation across `src/modules/research/` green; the strict isolated compile under `--lib ES2022` only (no DOM, no framework types) passes. Independent verification passed: 16 focused tests, the 67-test W1–W4b1 regression group, and the 290-test cross-module/layer group are green. The user explicitly accepted W4b1's documented size exception (~722 added lines): its application contract, runtime proof, barrel, and tracker evidence are cohesive. Work-unit commit: `f1dcd0b feat(research): add docx viewer dispatcher contract`. The ODD-MIGRATE-002 task bullet stays open because W4b2–W4b4 contracts remain. W4b2 is now active: only XLS/XLSX pure source and offline outcomes, with the legacy SheetJS CDN pin represented as typed metadata; React/CDN loading, DOM/HTML injection, and all other preview families remain out of scope.
  - Progress: W4b2 viewer-dispatch contract is implemented by this work unit: `src/modules/research/application/renderers.ts` extends the pure application-layer viewer-dispatch contract with two new `ViewerDispatch` variants (`sheet-source` and `sheet-offline`) covering both XLS and XLSX, carrying the typed source descriptor + bytes + pinned SheetJS CDN URL + window-global name so a future React mount (W6+) can load the legacy-pinned xlsx library via Next 16's `<Script src={scriptUrl} strategy="afterInteractive" onLoad={convert} onError={…}>` (see `node_modules/next/dist/docs/01-app/03-api-reference/02-components/script.md`) and call `window[scriptGlobal].read(bytes, {type: "array"})` + `window[scriptGlobal].utils.sheet_to_html(sheet)`. The application layer stays framework-free, browser-free, and CDN-loader-free — SheetJS is NOT imported or loaded here; the dispatcher only emits the typed source descriptor for the mount to consume. The W4b2 contract adds:
    - `SHEETJS_CDN_URL: string` — pinned CDN URL (`https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js`) mirroring `web/file_viewer.js::CDN_URLS.XLSX` + `web/index.html`'s matching `<script>` tag (whose inline comment calls the URL "Pinned URL: do not unpin." + flags the Apache 2.0 Community edition). Pinned byte-for-byte; bumping requires updating this constant AND the `<script>` tag AND the focused test that pins the literal.
    - `SHEETJS_GLOBAL_NAME: string` — the literal `"XLSX"` global name SheetJS assigns itself once the CDN loads. Mirrors the legacy `window.XLSX.read` + `window.XLSX.utils.sheet_to_html` call sites + the `CDN_URLS.XLSX` map key.
    - `"sheet-source"` `ViewerDispatch` variant — typed source descriptor carrying `src` (descriptor URL verbatim), `title` (descriptor name), `bytes` (the SAME `Uint8Array` reference as the input — pass-by-reference contract, the future mount reads bytes at mount time, no copy), `scriptUrl` (`SHEETJS_CDN_URL`), `scriptGlobal` (`SHEETJS_GLOBAL_NAME`). Emitted when `format === "xls" || format === "xlsx"` + `tab === "Raw"` + `bytes !== null`. Both XLS and XLSX dispatch through the same SheetJS path — the format field carries the extension verbatim so the mount can branch on XLS vs XLSX for format-specific affordances if needed.
    - `"sheet-offline"` `ViewerDispatch` variant — typed offline fallback carrying `name` (descriptor name), `download: ViewerLink` (the W4a pattern), `scriptUrl`, `scriptGlobal`, `reason: "bytes-missing"` (typed literal — the only offline path the dispatcher can detect at dispatch time, since CDN-load + read + sheet_to_html failures happen at the mount). Emitted when `format === "xls" || format === "xlsx"` + `tab === "Raw"` + `bytes === null`. Mirrors the legacy `web/file_viewer.js::renderOfflineBanner` recovery path so the future mount paints the same "Viewer offline — raw download available for X" banner verbatim. Does NOT carry a free-form `message` string — the mount owns the wording.
    - `case "xls":` + `case "xlsx":` switch arms — added BEFORE the `case "other":` and `default:` arms. The format switch now dispatches XLS / XLSX explicitly; EPUB / CSV / TSV / JSON remain in the default arm until W4b3+ land.
    - `buildSheetOffline(file)` helper — constructs the `sheet-offline` branch with `reason: "bytes-missing"`. Mirrors the W4b1 `buildDocxOffline(file)` helper verbatim.
    The Table/Tree gate still short-circuits before the format switch, so XLS / XLSX on Table/Tree still hits the `tab-not-applicable` branch (XLS / XLSX have a Raw-only renderer in this contract — SheetJS emits HTML tables, not a dedicated Table or Tree widget; a future mount that wants a dedicated Table renderer would land as a separately authorized follow-up slice).
  - The public barrel `src/modules/research/index.ts` re-exports `SHEETJS_CDN_URL` and `SHEETJS_GLOBAL_NAME` (as values) so cross-module consumers reach the W4b2 CDN pin through the barrel (spec.md rule 5). The two new variants live inside the existing `ViewerDispatch` discriminated union (no new top-level types) — the W4b3+ slices follow the same pattern.
  - `tests/test_research_renderers.py` extends the W4b1 test surface with 3 new focused tests: `test_renderers_file_has_no_sheetjs_renderer_or_loader_imports` (pins the no-xlsx-import, no-`loadScriptOnce`, no-`<script>`-injection, no-`XLSX.read`, no-`XLSX.utils`, no-`sheet_to_html`, no-`SheetNames` purity contract — the dispatcher only emits the typed source descriptor), `test_renderers_file_exports_named_sheetjs_cdn_url` (pins the `SHEETJS_CDN_URL` export + the legacy-pinned URL literal), `test_renderers_file_exports_named_sheetjs_global_name` (pins the `SHEETJS_GLOBAL_NAME` export + the `"XLSX"` literal). The barrel re-export test is extended to include the two new constants. The runtime harness removes XLS / XLSX from the W4a-deferred Raw-tab loop (step 20 — only EPUB / CSV / TSV / JSON remain deferred) and adds 12 new steps (38–49) that exercise: XLSX + Raw + bytes → `sheet-source` with descriptor + bytes + pinned URL + global name; XLS + Raw + bytes → `sheet-source` (verify both extensions dispatch the same way); XLSX + Raw + bytes=null → `sheet-offline` with download + pinned URL + global name + `reason: "bytes-missing"`; XLS + Raw + bytes=null → `sheet-offline` (verify both extensions dispatch the same way); XLS / XLSX on Table/Tree → `tab-not-applicable` (Table/Tree gate preserved for both extensions); no free-form `message` field on the offline variants (mirrors the W4b1 DOCX offline-shape contract); XLSX dispatcher purity on both source + offline branches; XLSX bytes-reference contract (the dispatched `bytes` is the SAME `Uint8Array` reference as the input — pass-by-reference, mirrors the W4b1 DOCX contract); bytes mutation visible through the dispatched reference (the dispatcher does NOT copy); XLS path purity (same dispatcher arm, but verify the XLS format field is preserved end-to-end). The W4b1 DOCX runtime coverage (steps 30–37) stays intact; the W4a SVG sanitizer's every branch (paired + self-closing `<script>` + case-insensitive `on*=` attrs) stays intact; the Markdown-as-text behavior is preserved (step 7 still asserts MD → text-pre with UTF-8 body verbatim); the legacy DOCX bytes-missing framing stays intact (step 32).
  - Evidence (this work unit): the implementation lands + the hermetic test harness passes under the focused pytest run; 19/19 focused W4a + W4b1 + W4b2 tests green (13 W4a + 3 W4b1 + 3 W4b2); the project-wide strict TypeScript compilation across `src/modules/research/` green; the strict isolated compile under `--lib ES2022` only (no DOM, no framework types) passes. Independent verification passed: 19 focused tests, 126 Research module-regression tests, and the full test suite with only three pre-existing unrelated Search-grid failures. The user explicitly accepted W4b2's documented size exception (1,034 diff lines): its application contract, runtime proof, barrel, and tracker evidence are cohesive. Work-unit commit: `47432e3 feat(research): add spreadsheet viewer dispatcher contract`. The ODD-MIGRATE-002 task bullet stays open because W4b3 (EPUB) + W4b4 (CSV/TSV/JSON) contracts remain. The user selected one cohesive W4b3 EPUB slice: pure source/offline outcomes and pinned epubjs metadata only; the future React mount owns `<Script>`, `ePub(bytes.buffer)`, page navigation, and book cleanup.
  - Progress: W4b3 viewer-dispatch contract is implemented by this work unit: `src/modules/research/application/renderers.ts` extends the pure application-layer viewer-dispatch contract with two new `ViewerDispatch` variants (`epub-source` and `epub-offline`) carrying the typed source descriptor + bytes + pinned epubjs CDN URL + window-global name so a future React mount (W6+) can load the legacy-pinned epubjs library via Next 16's `<Script src={scriptUrl} strategy="afterInteractive" onLoad={mount} onError={…}>` (see `node_modules/next/dist/docs/01-app/03-api-reference/02-components/script.md`), call `window[scriptGlobal](bytes.buffer)` to construct the book, then own `book.renderTo(hostEl, ...)` mount + prev / next click handlers + module-scoped `_currentBook.destroy()` teardown on the NEXT open (mirrors the legacy `web/file_viewer.js::renderEpub` lifecycle verbatim — the legacy tears down the previous book before mounting the new one so listeners don't leak per `design.md` §8). The application layer stays framework-free, browser-free, and CDN-loader-free — epubjs is NOT imported or loaded here; the dispatcher only emits the typed source descriptor for the mount to consume. The W4b3 contract adds:
    - `EPUBJS_CDN_URL: string` — pinned CDN URL (`https://cdn.jsdelivr.net/npm/epubjs@0.3.93/dist/epub.min.js`) mirroring `web/file_viewer.js::CDN_URLS.ePub` + `web/index.html`'s matching `<script>` tag (whose inline comment calls the URL "Pinned URL: do not unpin."). Pinned byte-for-byte; bumping requires updating this constant AND the `<script>` tag AND the focused test that pins the literal.
    - `EPUBJS_GLOBAL_NAME: string` — the literal `"ePub"` (lowercase `e`, capital `P`) global name the epubjs UMD bundle assigns itself to once the CDN loads. Mirrors the legacy `window.ePub(arrayBuffer)` call site + the `CDN_URLS.ePub` map key. The case is pinned — `window.epub` is undefined at runtime and would silently break the construction site.
    - `"epub-source"` `ViewerDispatch` variant — typed source descriptor carrying `src` (descriptor URL verbatim), `title` (descriptor name), `bytes` (the SAME `Uint8Array` reference as the input — pass-by-reference contract, the future mount reads bytes at mount time, no copy), `scriptUrl` (`EPUBJS_CDN_URL`), `scriptGlobal` (`EPUBJS_GLOBAL_NAME`). Emitted when `format === "epub"` + `tab === "Raw"` + `bytes !== null`.
    - `"epub-offline"` `ViewerDispatch` variant — typed offline fallback carrying `name` (descriptor name), `download: ViewerLink` (the W4a pattern), `scriptUrl`, `scriptGlobal`, `reason: "bytes-missing"` (typed literal — the only offline path the dispatcher can detect at dispatch time, since CDN-load + `ePub(arrayBuffer)` + `book.renderTo(...)` + `_currentBook.destroy()` failures happen at the mount). Emitted when `format === "epub"` + `tab === "Raw"` + `bytes === null`. Mirrors the legacy `web/file_viewer.js::renderOfflineBanner` recovery path so the future mount paints the same "Viewer offline — raw download available for X" banner verbatim. Does NOT carry a free-form `message` string — the mount owns the wording.
    - `case "epub":` switch arm — added BEFORE the `case "other":` and `default:` arms (right after the W4b2 `case "xls":` + `case "xlsx":` arms). The format switch now dispatches EPUB explicitly; CSV / TSV / JSON remain in the default arm until W4b4 lands.
    - `buildEpubOffline(file)` helper — constructs the `epub-offline` branch with `reason: "bytes-missing"`. Mirrors the W4b1 `buildDocxOffline(file)` + W4b2 `buildSheetOffline(file)` helpers verbatim.
    The Table/Tree gate still short-circuits before the format switch, so EPUB on Table/Tree still hits the `tab-not-applicable` branch (EPUB has NO Table/Tree renderer in this contract — epubjs renders an EPUB as a paged book, not a Table widget or a Tree widget; the future mount's EPUB viewer is the W4b3 source / offline surface itself, scoped to Raw).
  - The public barrel `src/modules/research/index.ts` re-exports `EPUBJS_CDN_URL` and `EPUBJS_GLOBAL_NAME` (as values) so cross-module consumers reach the W4b3 CDN pin through the barrel (spec.md rule 5). The two new variants live inside the existing `ViewerDispatch` discriminated union (no new top-level types) — the W4b4 slice follows the same pattern.
  - `tests/test_research_renderers.py` extends the W4b2 test surface with 3 new focused tests: `test_renderers_file_has_no_epubjs_renderer_or_loader_imports` (pins the no-epubjs-import, no-`loadScriptOnce`, no-`<script>`-injection, no-`ePub(...)` construction, no-`renderTo(...)` mount, no-`_currentBook` / `book.destroy()` lifecycle, no-`book.prev` / `book.next` navigation purity contract — the dispatcher only emits the typed source descriptor), `test_renderers_file_exports_named_epubjs_cdn_url` (pins the `EPUBJS_CDN_URL` export + the legacy-pinned URL literal), `test_renderers_file_exports_named_epubjs_global_name` (pins the `EPUBJS_GLOBAL_NAME` export + the `"ePub"` literal with the case-sensitive UMD global). The barrel re-export test is extended to include the two new constants. The runtime harness removes EPUB from the W4a-deferred Raw-tab loop (step 20 — only CSV / TSV / JSON remain deferred) and adds 8 new steps (50–57) that exercise: EPUB + Raw + bytes → `epub-source` with descriptor + bytes + pinned URL + global name `"ePub"` (NOT `"EPUBJS"` or `"epub"` — the case-sensitive UMD global); EPUB + Raw + bytes=null → `epub-offline` with download + pinned URL + global name + `reason: "bytes-missing"`; EPUB on Table/Tree → `tab-not-applicable` (Table/Tree gate preserved); EPUB dispatcher purity on both source + offline branches; EPUB bytes-reference contract (the dispatched `bytes` is the SAME `Uint8Array` reference as the input — pass-by-reference, mirrors the W4b1 DOCX + W4b2 XLS / XLSX contract); bytes mutation visible through the dispatched reference (the dispatcher does NOT copy); no free-form `message` field on the offline variant (mirrors the W4b1 DOCX + W4b2 XLS / XLSX offline-shape contract). The W4b1 DOCX runtime coverage (steps 30–37), W4b2 XLS / XLSX runtime coverage (steps 38–49), W4a SVG sanitizer coverage (paired + self-closing `<script>` + case-insensitive `on*=` attrs), and Markdown-as-text behavior preservation (step 7) all stay intact.
  - Evidence (this work unit): the implementation lands + the hermetic test harness passes under the focused pytest run; 22/22 focused W4a + W4b1 + W4b2 + W4b3 tests green (13 W4a + 3 W4b1 + 3 W4b2 + 3 W4b3); the 73-test W1–W4b3 regression group (20 W1 domain + 16 W2 application + 15 W3 infra + 22 W4a + W4b1 + W4b2 + W4b3 renderers) green; the project-wide strict TypeScript compilation across `src/modules/research/` green; the strict isolated compile under `--lib ES2022` only (no DOM, no framework types) passes. Independent verification passed: 22 focused tests, 67 focused Research/Explorer tests plus one skipped E2E test, and 1,536 passed full-suite tests with only three pre-existing unrelated Search-grid failures. The user explicitly accepted W4b3's documented size exception (1,108 diff lines): its application contract, runtime proof, barrel, and tracker evidence are cohesive. Work-unit commit: `b95018f feat(research): add epub viewer dispatcher contract`. The ODD-MIGRATE-002 task bullet stays open because W4b4 (CSV / TSV / JSON) contracts remain.
  - Progress: W4b4 viewer-dispatch contract is implemented by this work unit: `src/modules/research/application/renderers.ts` extends the pure application-layer viewer-dispatch contract with four new `ViewerDispatch` variants (`table-source`, `table-offline`, `json-source`, `json-offline`) covering CSV / TSV on Table + JSON on Tree. The W4b4 contract adds:
    - `PAPA_CDN_URL: string` — pinned Papa Parse CDN URL (`https://cdn.jsdelivr.net/npm/papaparse@5.4.1/papaparse.min.js`) mirroring `web/file_viewer.js::CDN_URLS.Papa` + the legacy `loadScriptOnce("Papa")` on-demand loader. Pinned byte-for-byte; bumping requires updating this constant AND the legacy map key AND the focused test that pins the literal.
    - `PAPA_GLOBAL_NAME: string` — the literal `"Papa"` global name Papa Parse assigns itself once the CDN loads. Mirrors the legacy `window.Papa.parse(text, { delimiter, skipEmptyLines: true })` call site + the `CDN_URLS.Papa` map key.
    - `TableDelimiter` — typed delimiter literal (`"," | "\t"`). The dispatcher computes the delimiter from the format field (`","` for CSV, `"\t"` for TSV — mirrors the legacy `renderTable` ternary `const delimiter = ext === "tsv" ? "\t" : ","`). The mount passes the delimiter straight to Papa's parse options.
    - `"table-source"` `ViewerDispatch` variant — typed source descriptor carrying `src` (descriptor URL verbatim), `title` (descriptor name), `bytes` (the SAME `Uint8Array` reference as the input — pass-by-reference contract, the future mount reads bytes at mount time, no copy), `scriptUrl` (`PAPA_CDN_URL`), `scriptGlobal` (`PAPA_GLOBAL_NAME`), `delimiter` (`TableDelimiter`). Emitted when `format === "csv" || format === "tsv"` + `tab === "Table"` + `bytes !== null`.
    - `"table-offline"` `ViewerDispatch` variant — typed offline fallback carrying `name` (descriptor name), `download: ViewerLink` (the W4a pattern), `scriptUrl`, `scriptGlobal`, `reason: "bytes-missing"` (typed literal). Emitted when `format === "csv" || format === "tsv"` + `tab === "Table"` + `bytes === null`. Mirrors the legacy `web/file_viewer.js::renderOfflineBanner` recovery path. Does NOT carry `delimiter` (the descriptor's `format` field carries "csv" or "tsv" verbatim so the mount can re-derive the delimiter from the format if it needs to retry the loader) and does NOT carry a free-form `message` string (mount owns the wording).
    - `"json-source"` `ViewerDispatch` variant — typed source descriptor carrying `src` (descriptor URL verbatim), `title` (descriptor name), `bytes` (the SAME `Uint8Array` reference as the input — pass-by-reference contract). Emitted when `format === "json"` + `tab === "Tree"` + `bytes !== null`. Carries NO CDN metadata (no `scriptUrl`, no `scriptGlobal`) — JSON parsing is native per the spec's "Tree viewer tab / No CDN is used." requirement, so there is no CDN URL to pin and no window-global to surface.
    - `"json-offline"` `ViewerDispatch` variant — typed offline fallback carrying `name` (descriptor name), `download: ViewerLink`, `reason: "bytes-missing"` (typed literal). Emitted when `format === "json"` + `tab === "Tree"` + `bytes === null`. Carries NO CDN metadata (mirrors the no-CDN shape of `json-source`). Does NOT carry a free-form `message` string.
    - `buildTableOffline(file)` helper — constructs the `table-offline` branch with `reason: "bytes-missing"`. Mirrors the W4b1 DOCX + W4b2 XLS / XLSX + W4b3 EPUB offline-helper shape verbatim.
    - `buildJsonOffline(file)` helper — constructs the `json-offline` branch with `reason: "bytes-missing"` and NO CDN metadata. Mirrors the W4b1 DOCX + W4b2 XLS / XLSX + W4b3 EPUB offline-helper shape verbatim.
    - The Table / Tree gate is now format-aware ONLY for the three W4b4 canonical exceptions — (Table, csv), (Table, tsv), (Tree, json) — every other Table / Tree combination still fires the legacy `${tab} view not available for .${ext} files — use Raw.` message. CSV / TSV on Raw still falls through to the existing W4a `unsupported` fallback / download behavior (the user decision is authoritative: Raw uses the existing fallback/download path). JSON on Raw stays on the existing W4a `unsupported` fallback / download behavior. CSV / TSV on Tree still fires `tab-not-applicable` (the canonical Table renderer does not apply to the Tree tab). JSON on Table still fires `tab-not-applicable` (the canonical Tree renderer does not apply to the Table tab).
  - The public barrel `src/modules/research/index.ts` re-exports `PAPA_CDN_URL` and `PAPA_GLOBAL_NAME` (as values) and `TableDelimiter` (as a type) so cross-module consumers reach the W4b4 CDN pin + delimiter typing through the barrel (spec.md rule 5). The four new variants live inside the existing `ViewerDispatch` discriminated union (no new top-level types) — the W4b4 slice follows the same pattern as W4b1 + W4b2 + W4b3.
  - `tests/test_research_renderers.py` extends the W4b3 test surface with 3 new focused tests: `test_renderers_file_has_no_papa_or_json_parse_imports` (pins the no-Papa-import, no-Papa.parse, no-`loadScriptOnce`, no-`<script>`-injection, no-`JSON.parse`, no-`JSON.stringify`, no-`Papa.unparse` purity contract — the dispatcher only emits the typed source / offline descriptor; JSON parsing is native and happens at the mount), `test_renderers_file_exports_named_papa_cdn_url` (pins the `PAPA_CDN_URL` export + the legacy-pinned URL literal + the version pin), `test_renderers_file_exports_named_papa_global_name` (pins the `PAPA_GLOBAL_NAME` export + the `"Papa"` literal). The barrel re-export test is extended to include the two new constants plus the `TableDelimiter` type re-export. The runtime harness updates step 27 to REMOVE CSV / TSV / JSON from the broader Table / Tree deferred loop (they now have explicit Table / Tree dispatchers) and adds 12 new steps (58–69) that exercise: CSV + Table + bytes → `table-source` with descriptor + bytes + pinned URL + global name + delimiter `","`; TSV + Table + bytes → `table-source` with delimiter `"\t"` (verify both extensions dispatch through the same `table-source` arm); CSV + Table + bytes=null → `table-offline` with download + pinned URL + global name + reason + NO `delimiter` + NO free-form `message`; TSV + Table + bytes=null → `table-offline` (verify both extensions dispatch through the same `table-offline` arm); CSV / TSV on Raw → `unsupported` with format-literal message (the user decision preserves the W4a fallback / download on Raw); CSV / TSV on Tree → `tab-not-applicable` (canonical-exception scope check: the W4b4 gate is format-aware ONLY for the three canonical pairings); JSON + Tree + bytes → `json-source` with descriptor + bytes ONLY (NO `scriptUrl`, NO `scriptGlobal`); JSON + Tree + bytes=null → `json-offline` with name + download + reason + NO CDN metadata + NO free-form `message`; JSON on Raw → `unsupported` (Raw uses the existing W4a fallback / download behavior); JSON on Table → `tab-not-applicable` (canonical-exception scope check); W4b4 dispatcher purity on all four new branches (CSV / TSV / JSON source + offline); W4b4 bytes-reference contract on `table-source` + `json-source` (the dispatched bytes IS the input bytes reference, by-reference, NO copy); W4b4 bytes-mutation visibility on `table-source` + `json-source` (mutating input bytes after dispatch is visible through the dispatched reference). The W4b1 DOCX + W4b2 XLS / XLSX + W4b3 EPUB runtime coverage stays intact; the W4a SVG sanitizer's every branch (paired + self-closing `<script>` + case-insensitive `on*=` attrs) stays intact; the Markdown-as-text behavior is preserved (step 7 still asserts MD → text-pre with UTF-8 body verbatim); the W4a Tab-not-applicable wording is preserved (steps 21–23 + the canonical-exception scope checks above); the W4a deferred Raw loop stays intact (step 20 still asserts CSV / TSV / JSON → `unsupported` on Raw per the user decision).
  - Evidence (this work unit): the implementation lands + the hermetic test harness passes under the focused pytest run; 25/25 focused W4a + W4b1 + W4b2 + W4b3 + W4b4 tests green (13 W4a + 3 W4b1 + 3 W4b2 + 3 W4b3 + 3 W4b4); the 76-test W1–W4b4 regression group (20 W1 domain + 16 W2 application + 15 W3 infra + 25 W4a + W4b1 + W4b2 + W4b3 + W4b4 renderers) green; the 299-test cross-module / layer / restricted-imports / domain-purity group (Research + Taxonomy + module layers + restricted imports + domain purity) green; the project-wide strict TypeScript compilation across `src/modules/research/` green; the strict isolated compile under `--lib ES2022` only (no DOM, no framework types) passes. Independent verification passed: 25 focused tests, the 76-test W1–W4b4 group, and strict project TypeScript compilation are green. The user explicitly accepted W4b4's documented size exception (1,593 diff lines): its application contract, runtime proof, barrel, and tracker evidence are cohesive. Work-unit commit: `393c736 feat(research): add table and JSON viewer contracts`. The ODD-MIGRATE-002 task bullet stays open because ODD-MIGRATE-003 (deliver the Research explorer and supported file-preview behavior in reviewable slices) remains. W4b4 closes the W4 split — every W4a–W4b4 family is now owned by the typed source / offline dispatch contract; the future W6 React mount owns parsing, JSON truncation, CDN script loading, and DOM / React / Next rendering. The user selected W6.1 as one cohesive, isolated `/explorer` route: the non-CDN Explorer/Viewer mount only, with no search, splitter, CDN viewers, browser-state expansion, legacy mutation, or cutover.
  - Progress: W6.5-BRIDGE-006 FolderTab → Explorer refresh bridge is implemented by this work unit. The W6.5 slice closes the remaining ODD-MIGRATE-003 deferred item by wiring a `window`-scoped `CustomEvent("taxa:explorer:refresh")` signal between the native `FolderTab` (in the taxonomy module) and the React Explorer route (in `@taxa/research`). The signal fires from `FolderTab` once the `materializeResearch` (or `openFolder`) POST reaches its success state (`createStatus.kind === "created"` / `openStatus.kind === "opened"`), and the Explorer route subscribes to the event on mount + unsubscribes on unmount so every dispatch re-fires `/api/files` without dropping the existing `ExplorerLoadStatus` / expanded set / selected-path / `ViewerState` / search state. The W6.5 contract preserves every W6.1 / W6.2 / W6.3 / W6.4a / W6.4b / W6.4c / W6.4d / W6.4e pin while adding the typed cross-route signal. The W6.5 contract adds:
      - `src/modules/research/presentation/explorer-state.ts` — W6.5 kernel additions. Three new exports land on the existing pure-typed state kernel (no React, no JSX, no fetch, no DOM, no localStorage, no process — the W6.1 framework-free purity contract is preserved verbatim):
        - `EXPLORER_REFRESH_EVENT_NAME: "taxa:explorer:refresh"` — the canonical verbatim literal constant. Pinned byte-for-byte against the user-specified event name. The kernel export is the canonical source for tests + cross-module consumers; the `Explorer.tsx` + `FolderTab.tsx` consumer literals are pinned to the same value via separate focused tests so a future PR that bumps the event name must update three places (kernel export + Explorer.tsx local const + FolderTab.tsx local const) AND re-run the W6.5 focused tests.
        - `FolderSuccessStatusKind` — the typed literal union `"created" | "opened"`. The two FolderTab status kinds that represent a successful transition (the `materializeResearch` POST returning a 2xx + `MaterializeResult` payload, OR the `openFolder` POST returning a 2xx + `OpenFolderResult` payload). Every other FolderCreateStatus / FolderOpenStatus kind (`idle` / `creating` / `opening` / `error` / `copied`) is intentionally excluded so the explorer refresh dispatches ONLY on the success transition — a mid-flight `"creating"` / `"opening"` state MUST NOT trigger the dispatch (the folder structure hasn't hit disk yet).
        - `isFolderSuccessStatusKind(kind: string): kind is FolderSuccessStatusKind` — pure TypeScript type-guard predicate. Returns `true` ONLY for the two success literals; returns `false` for every other status kind + `null` / `undefined` + case-mismatched would-be synonyms (`CREATED` / `succeeded` / `materialized`). The `kind is FolderSuccessStatusKind` return type lets the React layer narrow the literal without a follow-up cast. Pure, framework-free, importable through the kernel + barrel.
      - `src/modules/research/presentation/Explorer.tsx` — W6.5 subscription wiring. Two additions:
        - Local `EXPLORER_REFRESH_EVENT_NAME = "taxa:explorer:refresh"` constant (the Explorer.tsx-side literal pinned to match the kernel export).
        - A new `useEffect(() => { ... }, [loadTree])` block that subscribes via `window.addEventListener(EXPLORER_REFRESH_EVENT_NAME, handler)` on mount + returns a cleanup that calls `window.removeEventListener(EXPLORER_REFRESH_EVENT_NAME, handler)` on unmount. The handler body invokes `void loadTree()` — the existing W6.1 `useCallback` tree-fetch callback. The handler MUST NOT touch `setExpanded` / `setSelectedPath` / `setViewerState` / `setSearchQuery` / `setDebouncedQuery` / `setSearchMode` / `setSearchHideEmpty` so the bridge preserves ExplorerLoadStatus / expanded set / selected-path / ViewerState / search state on every FolderTab dispatch (the W6.5 acceptance criterion). The SSR guard (`typeof window === "undefined" return`) runs the existing W6.1 pattern verbatim. The effect dependency is `[loadTree]` (the stable callback wrapped in `useCallback([apiOrigin])`) so React dedupes across re-renders and re-subscribes only when `apiOrigin` changes — the listener identity check on add/remove matches exactly (same `(event, listener)` pair).
      - `src/modules/taxonomy/presentation/FolderTab.tsx` — W6.5 dispatch wiring. Three additions:
        - Local `EXPLORER_REFRESH_EVENT_NAME = "taxa:explorer:refresh"` constant (the FolderTab.tsx-side literal pinned to match the kernel export + Explorer.tsx literal).
        - `useEffect` added to the React import (the existing import was `{ Fragment }` + `type { ReactNode }` — the W6.5 dispatch lifecycle needs the hook for the two `useEffect`s that watch `createStatus` + `openStatus`).
        - Two new `useEffect`s:
          1. First effect: keyed on `[createStatus]`. Body: `if (typeof window === "undefined") return; if (createStatus.kind !== "created") return; window.dispatchEvent(new CustomEvent(EXPLORER_REFRESH_EVENT_NAME));`. The early-return on the non-success branch keeps the dispatch bound to the success transition only. The SSR guard skips the dispatch when running under SSR. The deps array `[createStatus]` means React's primitive-equality dedupe fires the dispatch exactly once per status transition (a re-render with the same `createStatus` reference does NOT trigger a duplicate dispatch).
          2. Second effect: keyed on `[openStatus]`. Identical shape with `openStatus.kind !== "opened"` as the success gate. Mirrors the first effect verbatim so the W6.5 contract is symmetric across the two side-effecting operations.
        - The two effects use the dispatch lifecycle pattern (event-name constant + early-return gate + typed CustomEvent construction + `window.dispatchEvent` call) so the dispatch is bound to the success transition only — a mid-flight `"creating"` / `"opening"` state MUST NOT fire the dispatch (the Explorer would refetch before the new folders hit disk).
      - The W6.5 work honours the isolation constraints verbatim:
        - No `@taxa/browser-state` key expansion (the W6.5 contract forbids it — the signal flows through a `window.CustomEvent`, the smallest surface that lets two separate routes cross-signal without a shared parent context or a browser-state key).
        - No router-key re-mount (the W6.5 contract forbids it — the Explorer route is NOT re-mounted on the dispatch; the existing instance re-fetches `/api/files` through its own `loadTree` callback).
        - No legacy `web/` mutation (FastAPI + the static-export pipeline + the legacy Browser-tab explorer remain untouched).
        - No `src/modules/research/index.ts` modification (the W6.5 brief forbids it — the kernel export lives in `explorer-state.ts` and the Explorer.tsx + FolderTab.tsx consumers pin the literal locally so the barrel stays untouched).
        - No `next-env.d.ts` modification.
        - No `src/modules/research/index.ts` barrel change (the W6.5 kernel additions surface through direct relative + local-literal imports so the public barrel is untouched).
        - No commit, push, PR, or work outside the declared edit surfaces. The pre-existing untracked `odd/tasks/g4-integration-delivery.md` + `odd/tasks/w6-3-record-and-w6-4-chain.md` files remain untouched.
  - Progress: W65-MANIFEST-008 AC-21 search-engine catalog consumer manifest is implemented by this work unit. The W65-MANIFEST-008 slice lands the hermetic pytest module that documents — in executable form — the runtime consumer surface of the legacy ``web/search_urls.js`` literal at the ODD-MIGRATE-004 boundary. The user-selected KEEP decision (W65-MAP-007 outcome) places the literal at ``web/search_urls.js`` until the W18 atomic cutover (ODD-MIGRATE-006); the manifest pins every active runtime consumer + every comment-only reference + the React taxonomy purity boundary + the server mirror integrity contract + the W18 carry-over record. The W65-MANIFEST-008 contract preserves every W6.5-BRIDGE-006 + W6.1 / W6.2 / W6.3 / W6.4a–W6.4e pin without modifying any production source. The W65-MANIFEST-008 contract adds:
      - ``tests/test_search_engine_consumer_manifest.py`` — a single hermetic pytest module (no FastAPI import, no Playwright, no DB, no network; reads files only via ``pathlib.Path``). 46 focused tests organised into eight test classes that document the AC-21 consumer surface end-to-end:
          - ``TestKeepDecision`` (4 tests) — the literal stays at ``web/search_urls.js`` until the W18 atomic cutover. Pins the file presence + the ``SEARCH_ENGINES`` + ``CATEGORIES`` named exports + the W65-MAP-007 / W65-MANIFEST-008 carry-over record in ``odd/tasks/w6-3-record-and-w6-4-chain.md``.
          - ``TestActiveRuntimeConsumers`` (7 tests) — pins every active runtime consumer of the literal: ``tests/test_smoke.py::test_search_engine_contract`` (AC-21 itself — verifies the test function + the ``open("web/search_urls.js")`` call site + the ``_SEARCH_ENGINES`` regex parse + the ``ast.literal_eval`` Python-side parse + the ``re.findall`` JS-side parse + the ``with_authorship`` flag comparison); ``tests/test_research_styles.py::LEGACY_SEARCH_URLS_JS`` Path constant + the ``test_search_tab_categories_render_in_fixed_order`` cascade-mirroring assertion; ``web/detail.js:24`` import (``import { SEARCH_ENGINES, CATEGORIES } from "./search_urls.js";`` verbatim); ``web/detail.js::renderSearchesTab`` function (the body iterates both literals + builds the ``new Map(SEARCH_ENGINES.map(...))`` O(1) engine-key lookup + renders the ``data-category`` attribute on the category header).
          - ``TestCommentOnlyReferences`` (1 parametrized test, 8 sites) — pins every comment-only reference the W65-MAP-007 map identified: ``src/app/globals.css:2063`` (PR 3c-c @layer components comment), ``src/modules/taxonomy/index.ts:139`` (ODD-TDS-001 isolation comment), ``src/modules/research/index.ts:13`` (research barrel future-relocation comment), ``api/server.py:682`` (AC-21 section header), ``api/server.py:1366`` (get_searches docstring), ``tests/test_research_styles.py:123 + :375`` (two category-order test doc-comments), ``tests/test_search_categories.py:141`` (browser-level grouping comment), ``tests/test_visible_taxonomy_tree.py:2668`` (visible-tree WebSocket inventory).
          - ``TestReactTaxonomyPurity`` (7 tests) — pins the React taxonomy isolation boundary. ``src/modules/taxonomy/presentation/search-categories.ts`` MUST NOT import the legacy literal (ODD-TDS-001 user constraint: "Do not introduce presentation→legacy-web dependency"); ``src/modules/taxonomy/presentation/SearchTab.tsx`` MUST NOT import the legacy literal; the bridge ships its own typed surface (``SEARCH_CATEGORIES`` + ``SEARCH_ENGINE_LIST`` + ``searchCategoryForEngine`` + ``resolveSearchEngineMeta``); the taxonomy barrel re-exports the React bridge but NOT the legacy literal; the research barrel does not import the legacy literal at runtime (the comment is documentation only).
          - ``TestServerMirrorIntegrity`` (7 tests) — pins the AC-21 byte-identical contract between ``api/server.py::_SEARCH_ENGINES`` and ``web/search_urls.js::SEARCH_ENGINES``: ``_SEARCH_ENGINES`` module-level constant exists; entry count matches; key order matches (drives the rendered button order inside each category via the React ``SEARCH_ENGINE_LIST`` bridge); label order matches (user-visible); ``with_authorship`` flag matches at every index (drives the server-side URL composition for BHL + Scholar); end-to-end index-aligned byte-identical round-trip; the AC-21 cross-file contract section header in ``api/server.py`` documents the test location.
          - ``TestAc21ContractRoundTrip`` (3 tests) — round-trip parity: the manifest mirrors the same regex+ast.literal_eval parse the AC-21 contract test uses, and verifies the parsed entries stay byte-identical end-to-end. A silent regex break would silently drift the search engine catalog — the manifest catches it.
          - ``TestW18CutoverCarryOver`` (4 tests) — pins the W18 atomic cutover record: ODD-MIGRATE-006 is defined as the W18 atomic cutover; ODD-MIGRATE-006 owns the legacy ``web/`` retirement; ``odd/tasks/w6-3-record-and-w6-4-chain.md`` enumerates W65-MANIFEST-008 + ``tests/test_search_engine_consumer_manifest.py``; the KEEP decision is recorded in the chain doc.
          - ``TestConsumerEnumeration`` (3 tests) — coverage surface: every active consumer the W65-MAP-007 map identified is pinned in the manifest; the ``COMMENT_REFERENCES`` tuple documents at least 5 comment-only sites; the module docstring names both AC-21 + the legacy detail consumer.
          - ``TestManifestSelfCheck`` (2 tests) — pins the hermeticity contract via ``ast.parse`` introspection (the manifest imports only ``ast``, ``re``, ``pathlib``, ``collections.abc``, ``pytest``, ``__future__``; no FastAPI, no Playwright, no requests, no urllib; no ``write_text`` / ``write_bytes`` / ``open`` / ``Popen`` / ``run`` calls outside the ``Path.read_text`` helper used by the self-check).
      - The W65-MANIFEST-008 work honours the isolation constraints verbatim:
          - No ``web/`` mutation (the literal stays in place per KEEP decision; the legacy ``detail.js`` import line is read-only).
          - No ``api/server.py`` mutation (the server mirror is read-only; the AC-21 contract is asserted via the existing regex + ``ast.literal_eval`` parse).
          - No React search-engine-port mutation (``search-categories.ts`` + ``SearchTab.tsx`` + ``taxonomy/infrastructure/api.ts`` stay untouched; the React purity is asserted via substring + AST checks).
          - No ``next-env.d.ts`` mutation.
          - No ``src/modules/research/index.ts`` mutation.
          - No ``odd/tasks/w6-3-record-and-w6-4-chain.md`` mutation.
          - No ``odd/tasks/g4-integration-delivery.md`` mutation.
          - No dependency changes + no installs.
          - No commit, push, or PR (parent owns the local commit authorization).
  - Evidence (this work unit): the manifest lands + RED observed for the two self-check tests (the initial substring-search approach matched the module's own docstrings + assertion messages) → GREEN after switching to ``ast.parse``-based introspection (the AST inspects only actual import nodes + actual call nodes, not the source text). 46/46 focused W65-MANIFEST-008 tests green; the 420-test W6.5 verification group (``pytest tests/test_research_explorer_mount.py tests/test_research_renderers.py tests/test_research_styles.py tests/test_research_domain.py tests/test_research_application.py tests/test_research_infra.py tests/test_taxonomy_*.py tests/test_smoke.py``) passes 420 + 8 pre-existing skipped; the project-wide strict TypeScript compile (``npx --yes -p typescript@5.7 tsc --noEmit --project tsconfig.json``) passes with no output and exit code 0. The manifest's hermeticity contract is enforced via AST introspection (no FastAPI, no Playwright, no requests, no urllib imports; no write-shaped calls). Every W65-MAP-007 finding is pinned: the KEEP decision, the four active runtime consumers (AC-21 + LEGACY_SEARCH_URLS_JS + detail.js:24 import + detail.js::renderSearchesTab body), the 8 comment-only references, the React taxonomy purity boundary (search-categories.ts + SearchTab.tsx + taxonomy barrel + research barrel all stay free of the legacy literal), and the W18 atomic-cutover carry-over (ODD-MIGRATE-006 owns the legacy ``web/`` retirement). Work-unit commit: pending user authorization (the parent owns the local commit authorization per the W65-MANIFEST-008 delivery strategy + the work-unit policy at the bottom of this tracker). The ODD-MIGRATE-004 task bullet stays open because the W18 atomic cutover (ODD-MIGRATE-006) is the only authorized boundary for retiring the legacy literal; until then, every consumer keeps referencing ``web/search_urls.js`` and the manifest pins each consumer + the server mirror integrity + the React purity boundary so a silent drift fails CI.
  - `tests/test_research_explorer_mount.py` extends the W6.4e surface with 7 new focused W6.5 tests + 1 W6.5 runtime harness:
      - `test_w65_kernel_exports_explorer_refresh_event_name_constant` (asserts `explorer-state.ts` exports `EXPLORER_REFRESH_EVENT_NAME` with the verbatim literal `"taxa:explorer:refresh"` — the canonical literal pin).
      - `test_w65_kernel_exports_is_folder_success_status_kind_predicate` (asserts `isFolderSuccessStatusKind` is a TypeScript type guard with the signature `(kind: string): kind is FolderSuccessStatusKind` — the predicate's negative surface is exercised by the runtime harness below).
      - `test_w65_explorer_defines_local_event_name_literal` (asserts `Explorer.tsx` defines the local literal `EXPLORER_REFRESH_EVENT_NAME = "taxa:explorer:refresh"` — the consumer-side literal pin).
      - `test_w65_explorer_subscribes_via_window_add_event_listener` (asserts `Explorer.tsx` calls `window.addEventListener(EXPLORER_REFRESH_EVENT_NAME, ...)` inside a `useEffect` block — verified by a lenient `useEffect(`-nearby heuristic that tolerates the SSR guard + handler arrow body + the `addEventListener` call site).
      - `test_w65_explorer_unregisters_via_window_remove_event_listener` (asserts `Explorer.tsx` calls `window.removeEventListener(EXPLORER_REFRESH_EVENT_NAME, ...)` — verified by a whitespace-stripped match that tolerates the implementation's newline + indent wrap; the addEventListener call site appears BEFORE the removeEventListener call site in the file).
      - `test_w65_explorer_listener_invokes_load_tree` (asserts the listener body invokes `loadTree()` — verified by scanning the `useEffect` block via brace-counting so the named handler definition + the addEventListener call site both fall in scope).
      - `test_w65_explorer_listener_does_not_touch_other_state` (asserts the listener body does NOT call `setExpanded` / `setSelectedPath` / `setViewerState` / `setSearchQuery` / `setDebouncedQuery` / `setSearchMode` / `setSearchHideEmpty` — verified by brace-counting the subscription `useEffect` block so the scan stays bounded to the effect body).
      - `test_compiled_w6_5_kernel_passes_runtime_contract` — the runtime harness reuses the existing `compiled_w6_1_kernel` fixture (the W6.1 kernel compile already pulls `explorer-state.ts` under `--lib ES2022` only, so the W6.5 additions land in the same compiled module without rebuilding). The harness exercises: `EXPLORER_REFRESH_EVENT_NAME` equals the verbatim literal `"taxa:explorer:refresh"`; `isFolderSuccessStatusKind("created")` returns `true`; `isFolderSuccessStatusKind("opened")` returns `true`; `isFolderSuccessStatusKind(<every-other>)` returns `false` for the full negative surface (`idle` / `creating` / `opening` / `error` / `copied` + empty string + case-mismatched synonyms like `CREATED` / `Created` / `succeeded` / `materialized` + the boolean-shaped strings `true` / `false` + the numeric strings `1` / `0` + `null` / `undefined`). The harness prints `PASS` on success; failure prints the offending assertion + the underlying error.
  - `tests/test_taxonomy_presentation_tree_state.py` extends the W6.5 surface with 6 new focused FolderTab dispatch tests:
      - `test_w65_folder_tab_defines_local_event_name_literal` (asserts `FolderTab.tsx` defines the local literal `EXPLORER_REFRESH_EVENT_NAME = "taxa:explorer:refresh"` — the dispatch-side literal pin).
      - `test_w65_folder_tab_dispatches_on_create_success` (asserts a `useEffect` keyed on `createStatus` branches on `createStatus.kind (===|!==) "created"` AND calls `window.dispatchEvent(new CustomEvent(EXPLORER_REFRESH_EVENT_NAME))` — verified via useEffect brace-counting so the success check + the dispatch call both fall in the same effect body).
      - `test_w65_folder_tab_dispatches_on_open_success` (asserts a `useEffect` keyed on `openStatus` branches on `openStatus.kind (===|!==) "opened"` AND calls `window.dispatchEvent(new CustomEvent(EXPLORER_REFRESH_EVENT_NAME))` — same brace-counting approach, scoped to the SECOND `useEffect` block in the file).
      - `test_w65_folder_tab_dispatches_via_window_only` (asserts the dispatch target is `window.dispatchEvent` and NOT `document.dispatchEvent` — the bridge target is `window` per the W6.5 contract).
      - `test_w65_folder_tab_dispatch_uses_new_custom_event_with_canonical_name` (asserts the dispatch uses `new CustomEvent(EXPLORER_REFRESH_EVENT_NAME)` AND does NOT inline the literal `"taxa:explorer:refresh"` in the constructor — the constant is the canonical source so a future rename updates one place).
      - `test_w65_folder_tab_dispatch_is_idempotent_via_status_kind_change` (asserts each `useEffect` is keyed on the discriminated union (`[createStatus]` / `[openStatus]`) so React's primitive-equality dedupe fires exactly once per status transition — verified via whitespace-stripped match that tolerates the implementation's newline + indent wrap on the deps array).
  - Evidence (this work unit): the implementation lands + the hermetic test harness passes under the focused pytest run. 13/13 focused W6.5 tests green (7 Explorer-mount + 6 FolderTab-dispatch); the 1 new W6.5 runtime harness green; the full regression suite per the W6.5 verification command (`pytest tests/test_research_explorer_mount.py tests/test_research_renderers.py tests/test_research_styles.py tests/test_research_domain.py tests/test_research_application.py tests/test_research_infra.py tests/test_taxonomy_*.py`) passes 413/413 (146 Explorer mount + 30 renderers + 54 styles + 20 domain + 16 application + 15 infra + 132 taxonomy = 413 = 400 pre-W6.5 baseline + 13 new W6.5 tests); the project-wide strict TypeScript compile (`npx --yes -p typescript@5.7 tsc --noEmit --project tsconfig.json`) passes with no output and exit code 0 (the W6.5 additions honour every W6.1 + W6.2 + W6.3 + W6.4a–W6.4e pin); the `node .pi/skills/impeccable/scripts/detect.mjs --json src/modules/taxonomy/presentation/FolderTab.tsx src/modules/research/presentation/Explorer.tsx` detector pass returns an empty findings array (`[]`) — no pre-existing Impeccable findings on the changed UI targets, no W6.5-introduced findings. The W6.1 + W6.2 + W6.3 + W6.4a + W6.4b + W6.4c + W6.4d + W6.4e tests from the prior work-unit commits remain green; the W6.5 runtime harness compiles the same `compiled_w6_1_kernel` fixture as the W6.1 + W64A + W64B + W64C + W64D + W64E harnesses, proving the kernel additions stay framework-free under `--lib ES2022` only. The dispatch + listener wiring covers every W6.5 acceptance criterion verbatim: `window`-scoped `CustomEvent` named `"taxa:explorer:refresh"`; dispatched from `FolderTab` once `materializeResearch` (or `openFolder`) reaches the success state; Explorer subscribes on mount + unsubscribes on unmount; re-fetches `/api/files` without dropping `ExplorerLoadStatus` / expanded set / selected-path / `ViewerState`; no `@taxa/browser-state` key expansion; no router-key re-mount; no legacy `web/` mutation; no `next-env.d.ts` edit; no `src/modules/research/index.ts` edit. The branch is purely about wiring the FolderTab → Explorer signal across two separate routes.

- [ ] ODD-MIGRATE-003 Deliver the Research explorer and supported file-preview behavior in reviewable slices.
  - Route: delegated worker; chained PRs expected.
  - Scope: `src/modules/research/` and focused tests, preserving legacy behavior.
  - Evidence: focused contracts and browser verification for explorer, materialization, open-folder, and each supported preview family.
  - Progress: W6.1 isolated `/explorer` route is implemented and independently verified. The W6.1 mount is the smallest non-CDN React Explorer/Viewer mount built on the completed W1–W4b4 contracts. Six new files + one barrel extension land on the W6.1 declared edit surfaces:
      - `src/app/explorer/page.tsx` — Server Component. Passes ONLY the serializable `apiOrigin: string` prop to the Explorer client island (a Server Component cannot pass repository functions to a Client Component — the technical correction the user surfaced). Mounts the typed `<Explorer>` through `@taxa/research` inside the existing `<AppShell>` from `@taxa/app-shell`. The page is the only Server Component in the W6.1 surface; the React mount is the boundary.
      - `src/modules/research/presentation/explorer-state.ts` — pure typed state kernel (no React, no JSX, no fetch, no DOM, no localStorage, no process). Compiles in isolation under `--lib ES2022` only. Owns the `ExplorerLoadStatus` discriminated union (idle / loading / loaded / empty / error), the `ViewerState` view-model, the `bytesRequiredForFormat(format)` predicate (true ONLY for TXT / MD / SVG — every other W4a family passes the URL straight through without reading bytes), the `castFileFormat(extension)` safe-extension fallback ("other" for unknown / null / empty), the `buildServeUrl(baseUrl, relativePath)` W3 URL builder (mirrors `web/file_explorer.js::serveUrl` byte-for-byte: URL-encode verbatim, trim trailing slash on baseUrl), the recursive `enumerateFiles(node)` walker, the pure `toggleExpansion(set, path)` + `withExpanded(set, paths)` Set transitions, and the re-export of the W1 domain types + `createInitialExplorerState` factory so the React layer never deep-imports `../domain/explorer`.
      - `src/modules/research/presentation/Explorer.tsx` — Client island (`"use client"` boundary at the top). Owns the lifecycle: `ExplorerLoadStatus` state, expanded-folders Set state, selected-path state, `ViewerState`. Fires `fetchFiles` (from `@taxa/research`) on first render with the same-origin `apiOrigin` prop. Wires the FileTree + Viewer children through stable `useCallback` references. Wraps the two-pane layout in the typed `ExplorerErrorBoundary`.
      - `src/modules/research/presentation/FileTree.tsx` — Client component (`"use client"`). Recursive folder/file rows. Folders render with a chevron + folder icon + `aria-expanded`; single-click selects the folder (highlight-only, mirrors `web/file_explorer.js::selectFolder`), chevron click toggles expansion, keyboard `ArrowRight` / `ArrowLeft` toggle expansion, `Enter` / `Space` activate selection. Files render with a description icon + basename + size; single-click selects (no network, mirrors `selectFile`), double-click opens (mirrors `openFile`), `Enter` opens, `Space` selects. `aria-expanded` + `data-folder-path` + `data-file-path` carry the typed state through DOM. `.selected` class toggles highlight without a re-render (matches the legacy DOM-only selection shape).
      - `src/modules/research/presentation/Viewer.tsx` — Client component (`"use client"`). Owns the bytes-fetch lifecycle for TXT / MD / SVG. Calls `dispatchViewer({file, tab, bytes})` and switches on the typed `ViewerDispatch` outcome. Renders the W4a non-CDN variants (`pdf-iframe`, `html-iframe` with `sandbox=""` (NO `allow-same-origin`), `text-pre` (UTF-8 body verbatim), `image` (with the legacy 50 MB advisory), `image-error`, `svg-sanitized` (the W4a scrub is the XSS guard — `dangerouslySetInnerHTML` is intentional and pinned by the test surface), `video` (controls + preload=metadata), `unsupported` (with the format-literal "Format .xyz not supported in viewer." message + download link), `tab-not-applicable` (with the legacy "${tab} view not available for .${ext} files — use Raw." wording pinned via `TAB_NOT_APPLICABLE_SUFFIX`). The W4b1–W4b4 source / offline variants (DOCX / XLS / XLSX / EPUB / CSV / TSV / JSON) surface through a single `cdn-pending` offline banner with a download affordance — the W6.1 mount is non-CDN by design, so the CDN libraries are not wired; the user always has a recovery path to the raw file. Tab buttons dispatch back through `onTabChange` so the active tab is owned at the Explorer level.
      - `src/modules/research/presentation/ExplorerErrorBoundary.tsx` — Client component (`"use client"`). React class boundary. `getDerivedStateFromError` flips state; `componentDidCatch` logs to console; `retry()` flips back to a clean render. Inline fallback paints a `role="alert"` card with the error message + "Try again" button.
      - `src/modules/research/index.ts` — public barrel extension. Adds: W1 domain type re-exports (`ExplorerTree`, `ExplorerTreeNode`, `ExplorerFolderNode`, `ExplorerFileNode`, `ViewerTab`, `FileFormat`, `SearchState`, `ExplorerState`) + the W1 `createInitialExplorerState` factory (the React layer's typed hand-off to the domain shape); the W6.1 kernel helpers + types (`createInitialLoadStatus`, `createInitialViewerState`, `bytesRequiredForFormat`, `castFileFormat`, `buildServeUrl`, `enumerateFiles`, `toggleExpansion`, `withExpanded` + the `ExplorerLoadStatus` / `ViewerState` types); the four React components as default exports (`Explorer`, `Viewer`, `FileTree`, `ExplorerErrorBoundary`). The barrel is the SINGLE typed hand-off surface — spec.md rule 5 keeps every consumer anchored at `@taxa/research`.
  - The W6.1 work honours the isolation constraints verbatim:
      - `/explorer/page.tsx` is the ONLY new route; the existing `src/app/page.tsx` (taxonomy single-screen entry) is untouched.
      - Legacy `web/` directory is untouched; FastAPI is untouched; the static-export pipeline is untouched.
      - No search, no splitter, no materialization, no browser-state keys (no `@taxa/browser-state` import), no `<Script>` CDN loader, no CDN-script surface (the W4b1–W4b4 source variants surface through the offline banner — a future W6+ slice that wires the CDN libraries would land as a separately authorized follow-up).
      - No new dependencies added (`package.json` untouched).
      - No commit, push, PR, or work outside the declared edit surfaces.
  - `tests/test_research_explorer_mount.py` adds 48 focused hermetic pytest contract tests: 6 file-presence guards (one per W6.1 file, with the `.tsx` / `.ts` extension contract pinned), 4 Server-Component purity guards (no `"use client"` on `page.tsx`, only serializable props, isolation from `src/app/page.tsx` + `web/` + app-shell internals + FastAPI / CDN / direct `fetch(`), 4 Client-Component boundary guards (every React component declares `"use client"` above the imports), 26 framework-free kernel purity guards (no React / Next / FastAPI / Starlette / `fetch(` / DOMParser / document / window / localStorage / process / require / globalThis / cross-layer / cross-module tokens; comments stripped before scanning), 3 kernel contract guards (only domain + application imports, named function exports, named type-or-interface exports), 4 barrel re-export guards (the four React components as default exports + the 8 kernel helpers + the 2 kernel types + the W1 domain types all reachable through `@taxa/research`), 1 strict isolated TypeScript compile + Node runtime harness (asserts `toggleExpansion` round-trips a fresh Set, `withExpanded` adds every folder, `bytesRequiredForFormat` matches the W6.1 contract for every W4a family, `castFileFormat` falls back to "other" for unknown / null / empty / case-mismatched extensions, `buildServeUrl` URL-encodes verbatim + trims trailing slash, `enumerateFiles` yields every file in depth-first pre-order), and 1 project-wide strict TypeScript compile (`tsc --noEmit --project tsconfig.json` with the project's full strict flag set + `noUncheckedIndexedAccess` + `noUnusedLocals` + `noUnusedParameters` + `noImplicitReturns` + `noFallthroughCasesInSwitch`).
  - Evidence (this work unit): 49/49 focused W6.1 tests green after the folder-selection correction; the 76-test W1–W4b4 regression group remains green; project-wide strict TypeScript compilation is green; Next 16.3.5 static build passes and prerenders `/explorer` as a static route. The user explicitly accepted W6.1's documented ~3,064-line size exception: route, client island, pure state kernel, public barrel, and runtime proof are cohesive. Work-unit commit: `1748255 feat(research): add explorer route`. Independent verification pending — the parent must run `pytest tests/test_research_explorer_mount.py tests/test_research_domain.py tests/test_research_application.py tests/test_research_infra.py tests/test_research_renderers.py` + `npx tsc --noEmit --project tsconfig.json` independently before this evidence flips to "verified". The user explicitly accepted the W6.1 size exception (~1,000 lines across 6 files + 48 tests + barrel extension + tracker evidence) because the mount, its runtime proof, the focused tests, and the tracker evidence are cohesive — splitting would separate behavior from proof. Work-unit commit: pending user authorization. The ODD-MIGRATE-003 task bullet stays open because the explorer + the W6.2 search + the W6.3+ materialization / open-folder / each preview family remain. The W6.1 mount is reviewable in isolation; future slices add search / splitter / materialization / CDN viewers through separately authorized follow-up PRs.
  - Progress: W6.2 Browser-tab tree search is implemented by this work unit. The W6.2 slice extends the W6.1 mount with the search semantics the legacy `web/file_explorer.js` shipped byte-for-byte (200 ms debounce, case-insensitive substring match on `name` + `path`, filter mode hides non-matches + auto-expands ancestors, highlight mode toggles `search-match` without touching expansion, clear restores the tree, `filter + hideEmpty + zero matches` paints the exact `No matches.` card, Escape clears, mode + hide-empty controls toggle state, conditional auto-focus fires only when `document.activeElement === document.body`). The slice is split into a pure typed kernel + a single React-layer pass that handles the debounce + the conditional auto-focus + the DOM-mutation passes; no new port, adapter, dependency, or browser-state key ships with W6.2. The W6.2 contract adds:
      - `src/modules/research/presentation/explorer-state.ts` — W6.2 extension. Pure typed state kernel adds:
        - `SearchAnnotation` interface (`{ matches: ReadonlySet<string>, ancestors: ReadonlySet<string> }`). The W1 `SearchState` (query / mode / hideEmpty) re-export joins the kernel's public surface so the React layer never deep-imports `../domain/explorer`.
        - `createEmptySearchAnnotation()` pure factory — returns a fresh `{matches, ancestors}` per call so a consumer can mutate locally without bleeding into a sibling.
        - `annotateMatches(rootNode, query)` pure helper — iterative pass 1 walks the recursive tree and adds every node whose `name` OR `path` contains the lower-cased needle into `matches`. Recursive pass 2 walks post-order and promotes every folder with at least one matching descendant into `ancestors`. The synthetic-root `path === ""` guard prevents the wire tree root from appearing in `ancestors` (matches the legacy `_annotateMatches` shape). Empty / whitespace-only queries return the empty annotation (the React layer's `useMemo` short-circuits without burning the walker). Compiles in isolation under `--lib ES2022` only.
      - `src/modules/research/presentation/FileTree.tsx` — W6.2 extension. The component accepts three new props from Explorer (`searchAnnotation: SearchAnnotation | null`, `searchMode: "filter" | "highlight"`, `searchHideEmpty: boolean`). A `useEffect` keyed on the typed annotation runs the legacy `applySearchToTree` / `applyHighlightToTree` / `restoreTree` / `showSearchEmpty` / `hideSearchEmpty` passes via DOM lookup on the rendered tree's root ref (mirrors the legacy `applySearchToTree(host, annotation)` shape verbatim — filter mode hides non-matches + auto-expands ancestors, highlight mode toggles `search-match` without touching expansion, restore un-hides + clears `.search-match`, `filter + hideEmpty + zero matches` paints the exact `No matches.` card). The existing recursive renderer stays untouched; the search effects run after the React commit phase so a fast-typing user never blocks on a stale mutation pass. The internal `cssEscape` polyfill mirrors the legacy `web/file_explorer.js::cssEscape` shape so paths with spaces / accents / brackets round-trip through `querySelector('[data-folder-path="..."]')`.
      - `src/modules/research/presentation/Explorer.tsx` — W6.2 extension. The component owns the search surface: `searchQuery` (live, pre-debounce), `debouncedQuery` (200 ms delayed — the legacy `setTimeout(..., 200)` shape preserved verbatim), `searchMode` (`"filter"` default), `searchHideEmpty` (default `true` — mirrors the W1 `createInitialExplorerState().search`). A 200 ms debounce `useEffect` (with `clearTimeout` cleanup so a race between a delayed commit and a re-render never fires a stale annotation). A conditional auto-focus `useEffect` runs `searchInputRef.current?.focus({ preventScroll: true })` inside a `requestAnimationFrame` ONLY when `document.activeElement === document.body` (the legacy `web/file_explorer.js::renderTreeHeader()` focus check preserved verbatim so a mid-IME-composition user never has their keystroke stolen). A `useMemo` computes the typed `SearchAnnotation` from the debounced query + the loaded tree's root (returns `null` when the tree isn't loaded or the query is empty so the FileTree's `useEffect` short-circuits with the restore pass). The new `renderSearchHeader()` JSX renders the input + clear button + mode toggle + hide-empty toggle inside `.fex-tree-header-search` underneath the toolbar — the block stays always visible so the user can clear the input even when the tree is empty / loading / errored. Escape clears via the legacy `keydown` branch (the input retains focus through the re-render so the legacy "without losing focus" contract holds). The mode toggle swaps the icon + `aria-pressed`; the hide-empty toggle updates state + re-applies the filter pass (no-op in highlight mode).
      - `src/modules/research/index.ts` — public barrel extension. Adds the W6.2 typed surface: `annotateMatches` + `createEmptySearchAnnotation` (as values) and `SearchAnnotation` (as a type). The barrel is the SINGLE typed hand-off surface so the React layer's only import is `@taxa/research` + the kernel — spec.md rule 5 forbids reverse deep imports into the presentation layer.
      - `src/app/globals.css` — full Browser-tab explorer cascade migration. The legacy `web/index.html::.fex-*` inline `<style>` block migrates byte-equal into `@layer components` so the React `/explorer` mount renders with the same visual contract as the legacy oracle. The cascade covers: `.fex-banner` (CDN-failure banner), `.fex-children` (vertical guide line), `.fex-csv-scroller` + `.fex-csv-table` (+ `thead th` + `tbody td` + `:nth-child(even)` zebra + `:hover` row tint — Table viewer), `.fex-empty-state` (+ `.fex-empty-state-icon`), `.fex-image` + `.fex-image-advisory` + `.fex-image-frame`, `.fex-json-*` (caret / children / key / node / summary / tree — JSON viewer), `.fex-meta-spacer` + `.fex-meta-strip`, `.fex-row` (+ `:hover` + `:focus-visible` + descendant `fex-chevron` / `fex-icon` / `fex-label` / `fex-meta` + `.fex-row-wrap` + `.fex-row.file.selected` + `.fex-row.folder.selected` + `[data-realm="..."]` + `[data-realm]` + `.fex-row.search-match`), `.fex-search-*` (W6.2: input + `:focus` ring + clear + `:hover` + `Empty` placeholder + `hide-empty-btn` + `[aria-pressed="true"]` + `icon` + `input` + `:not(:placeholder-shown) ~ .fex-search-clear` + `mode-btn` + `[aria-pressed="true"]` + `row` + `toggles`), `.fex-shell` (two-pane layout), `.fex-snippet-*` (frame + title + dots + `.dot-r` / `.dot-y` / `.dot-g` + body + actions + btn + `:hover` + `:disabled`), `.fex-tab-strip` (+ button + button.active), `.fex-tree-header` (+ `h2`) + `.fex-tree-header-search` (W6.2), `.fex-tree-leaf` (+ `.type-boolean` / `.type-meta` / `.type-null` / `.type-number` / `.type-string`), `.fex-tree-pane`, `.fex-tree-truncated` (+ `.material-symbols-outlined`), `.fex-video-el` + `.fex-video-frame`, `.fex-viewer-pane`. Every selector uses the project's design tokens (`--surface`, `--surface-container-low`, `--outline-variant`, `--primary`, `--on-surface-variant`, `--realm-*`) so the cascade flips with the dark-mode `[data-theme="dark"]` override via the var() indirection. The cascade respects the chain-topology + alphabetic contract — the alphabetic ordering inside the `.fex-*` family mirrors the PR 3c-b convention (base → descendants → child → different base → period modifier → attribute modifier). Every `.fex-*` selector lives under `@layer components`, not `@layer base`, so the Tailwind 4 utility surface (PR 3c-e) can still override.
      - `tests/test_research_styles.py` — extension. Adds `FEX_EXPLORER_BASES` (the 41 unique `.fex-*` base selectors the chain-topology guard whitelists). The chain-topology guard's allowed set now unions `TAXONOMY_OWNED_BY_3C_B` + `UTILITY_CLASSES_OWNED_BY_3C_E2` + `FEX_EXPLORER_BASES` + the existing research/chrome + taxonomy tabs (`.folder-tab`, `.header-browser-tab`, `.research-explorer`, `.search-tab`, `.synonym-tab`, `.vernacular-tab`, `.distribution-tab`). The alphabetic ordering test (`test_top_level_selectors_are_alphabetically_ordered`) now covers the W6.2 cascade too — every `.fex-*` selector sorts into the right position with the pseudo-stripped comparison the test enforces.
      - `tests/test_research_explorer_mount.py` — extension. The runtime harness extends with 4 new focused W6.2 steps (steps 7–10) that exercise the W6.2 pure kernel helpers under Node's ES2022-only environment: `createEmptySearchAnnotation` returns fresh + empty Sets; `annotateMatches(tree, "")` / `(tree, "   ")` / `(null, "Mammalia")` all return the empty annotation (the React layer's `useMemo` short-circuit contract); `annotateMatches(tree, "mammalia")` matches Mammalia.pdf by name + promotes the Animalia folder into `ancestors` (post-order contract); `annotateMatches(tree, "rosa")` matches Rosa.md by name + promotes Plantae into `ancestors` (post-order ancestor promotion even when the folder's own name does NOT contain the needle); `annotateMatches(tree, "Animalia")` matches the Animalia folder + the two descendants by path substring (the legacy `_annotateMatches` "name OR path" contract); `annotateMatches` with a query containing spaces + accents round-trips through the path substring match byte-for-byte. The kernel named-exports test extends to include `annotateMatches` + `createEmptySearchAnnotation`; the kernel named-type-exports test extends to include `SearchAnnotation`; the barrel re-export contract tests extend to include the W6.2 values + type.
  - The W6.2 work honours the isolation constraints verbatim:
      - `/explorer/page.tsx` is untouched (the existing W6.1 route is unchanged).
      - Legacy `web/` directory is untouched; FastAPI is untouched; the static-export pipeline is untouched.
      - No materialization, no splitter, no browser-state keys (no `@taxa/browser-state` import), no `<Script>` CDN loader, no new dependencies added (`package.json` untouched).
      - No commit, push, PR, or work outside the declared edit surfaces.
  - Evidence (this work unit): 155/155 focused Research tests green (49 W6.1 mount + 30 styles + 76 W1–W4b4 regression); the strict isolated TypeScript compile under `--lib ES2022` only passes; the project-wide strict TypeScript compile (`tsc --noEmit --project tsconfig.json`) passes; the alphabetic + chain-topology + allow-list tests for the `.fex-*` cascade are green. Independent verification pending — the parent must run the focused pytest group + the strict typecheck independently before this evidence flips to "verified". The user explicitly accepted the W6.2 documented size exception: kernel helpers + DOM mutation helpers + Explorer search state + search-block JSX + barrel extension + CSS migration (full `.fex-*` cascade) + tracker evidence are cohesive because splitting would separate the search surface from its runtime proof. Work-unit commit: pending user authorization. The ODD-MIGRATE-003 task bullet stays open because W6.3+ materialization / open-folder / each preview family / splitter / etc. remain. The W6.2 search is reviewable in isolation; the `web/file_explorer.js` legacy search oracle is mirrored byte-for-byte so a future UX iteration can A/B compare by side-loading the W6.2 route against the legacy Browser tab without functional surprises.
  - Progress: W6.3 Explorer splitter is implemented by this work unit. The W6.3 slice extends the W6.1 + W6.2 mount with the Browser-tab tree/viewer drag-handle splitter, mirroring the legacy `web/file_explorer.js::renderSplitter()` byte-for-byte: mouse drag adjusts the tree width in real time; drag state shows `col-resize` cursor + the `dragging` class so the cascade paints the primary-color hover affordance; width clamps to `12rem` minimum and `shellWidth - 20rem` maximum (legacy `MIN_WIDTH` + `MAX_WIDTH` shape verbatim); mouseup persists the final tree-pane width to the raw localStorage key `taxa.fex.treeWidth` (legacy `TREE_WIDTH_STORAGE_KEY` shape verbatim); initial mount restores that value + clears the legacy CSS `max-width` cap so the saved width isn't bounded by `min(60%, 50rem)`; double-click removes the key + clears the inline `width` + `maxWidth` so the legacy CSS default takes over again; all localStorage failures are swallowed so the splitter still works in private-browsing / disabled-storage contexts. The Splitter is intentionally DOM-bound (NOT the framework-free kernel): it walks the DOM from its `parentElement` + manipulates the `.fex-tree-pane` sibling via class selector, attaches document-level mousemove + mouseup listeners (the user can drag outside the splitter's bounding box), and reads / writes / removes the single global localStorage key `taxa.fex.treeWidth`. The pure helpers (`clampTreeWidth`, `readSavedTreeWidth`, `writeSavedTreeWidth`, `clearSavedTreeWidth`) + the width-bound constants (`MIN_TREE_WIDTH_PX = 12 * 16`, `VIEWER_RESERVED_PX = 20 * 16`) + the storage key constant (`TREE_WIDTH_STORAGE_KEY = "taxa.fex.treeWidth"`) are named exports on the same module so the focused test harness exercises them under Node without React or the DOM event system. The W6.3 contract adds:
      - `src/modules/research/presentation/Splitter.tsx` — Client Component (`"use client"` boundary at the top). DOM-bound drag handle between the tree pane + the viewer pane. Mirrors the legacy `web/file_explorer.js::renderSplitter()` byte-for-byte. Exports the pure helpers + constants as named exports; the default export is the `Splitter` React component. The component attaches mousedown + dblclick handlers; the mousedown handler attaches document-level mousemove + mouseup listeners (cleaned up via a ref so a mid-drag route change doesn't leak listeners). The mount-restore `useEffect` (empty deps) reads the saved width + applies it to the tree pane on first render; the unmount-cleanup `useEffect` drops any in-flight drag listeners. The component is zero-prop by default (it walks the DOM from its `parentElement` via `querySelector(".fex-tree-pane")`, mirroring the legacy shape verbatim — no prop drilling). The pure helpers swallow every error path: `readSavedTreeWidth` returns `null` on absent key + getItem throws + localStorage undefined; `writeSavedTreeWidth` no-ops on setItem throws + localStorage undefined; `clearSavedTreeWidth` no-ops on removeItem throws + localStorage undefined. Accessibility: `role="separator"` + `aria-orientation="vertical"` + the legacy `title` literal `"Drag to resize · double-click to reset"` (the `\u00b7` middle-dot character is pinned byte-for-byte against the legacy).
      - `src/modules/research/presentation/Explorer.tsx` — W6.3 extension. The component mounts `<Splitter />` between the `.fex-tree-pane` sibling + the `.fex-viewer-pane` sibling (the Splitter's `parentElement.querySelector(".fex-tree-pane")` lookup depends on both panes being direct siblings under the `.fex-shell` container — the W6.1 mount shape). The Splitter reaches the public barrel through `import { Splitter } from "@taxa/research"` (spec.md rule 5 keeps cross-module imports anchored at the public barrel).
      - `src/modules/research/index.ts` — public barrel extension. Adds the W6.3 typed surface: `Splitter` (default export re-export so the React mount reaches it through `import { Splitter } from "@taxa/research"`) + `TREE_WIDTH_STORAGE_KEY` + `MIN_TREE_WIDTH_PX` + `VIEWER_RESERVED_PX` + `clampTreeWidth` + `readSavedTreeWidth` + `writeSavedTreeWidth` + `clearSavedTreeWidth` (named re-exports for the focused test harness + future cross-module consumers). The storage helpers only touch the single `TREE_WIDTH_STORAGE_KEY` localStorage key — no scope creep into `@taxa/browser-state`, no domain state, no settings reset, no server surface, no CDN viewers, no materialization, no `web/` mutation (the W6.3 isolation contract).
      - `src/app/globals.css` — Browser-tab explorer cascade extension. Adds the W6.3 splitter selectors under `@layer components`: `.fex-splitter` (base — 4px transparent bar with `col-resize` cursor, `flex-shrink: 0`, `position: relative`), `.fex-splitter::after` (hit-area extension — `inset: 0 -3px` so the drag stays discoverable on the very-thin visual surface), `.fex-splitter:hover, .fex-splitter.dragging` (primary-color tint — `background: var(--primary)` — so the affordance lights up without forcing the user to commit), `.fex-splitter:focus-visible` (keyboard a11y ring — `outline: 2px solid var(--primary)` matching the existing `.fex-row:focus-visible` outline). The splitter selectors sit between `.fex-snippet-title` + `.fex-tab-strip` in the alphabetic chain (the W6.2 alphabetic-ordering contract is preserved). The chain-topology guard whitelists `.fex-splitter` through `FEX_EXPLORER_BASES` so the W6.2 alphabetic + chain-topology tests stay green.
      - `tests/test_research_explorer_mount.py` — extension. Adds the W6.3 splitter test surface (11 focused tests + 1 compile fixture + 1 runtime harness + 1 project-wide typecheck):
          - File-presence guard (parametrized on `SPLITTER_FILE`).
          - Client-Component boundary guard (parametrized to include Splitter).
          - Splitter-specific source-level purity guards (parametrized on the W6.3 forbidden-tokens list — no `fetch(`, no framework imports other than React, no CDN-script surface, no reverse deep imports into any other layer of `@taxa/research`, no cross-module imports outside Research, no legacy + isolation tokens, no `process.` / `require(` / CDN-library tokens).
          - Import-surface guard (Splitter's only runtime import is `react`).
          - Named-export guard (the 7 pure helpers + constants).
          - Default-export guard (`export default function Splitter`).
          - Legacy-ARIA-semantics guard (`role="separator"` + `aria-orientation="vertical"` + the legacy `title` literal with the `\u00b7` middle-dot).
          - Mousedown-handler guard (`onMouseDown={handleMouseDown}` wired + `e.preventDefault()` inside the handler).
          - Doubleclick-handler guard (`onDoubleClick={handleDoubleClick}` wired).
          - Storage-key literal guard (`TREE_WIDTH_STORAGE_KEY` equals `"taxa.fex.treeWidth"`).
          - Barrel re-export guard (Splitter default + 7 pure helper / constant re-exports).
          - Explorer-mounts-splitter guard (Explorer.tsx imports Splitter from `@taxa/research` + renders it AFTER `.fex-tree-pane` JSX + BEFORE `.fex-viewer-pane` JSX).
          - Compile + runtime fixture: `_run_tsc_isolated_splitter(out_dir, sources)` compiles with `--lib ES2022,DOM --jsx react-jsx` (the Splitter uses `document.body`, `MouseEvent`, `globalThis.localStorage`, and JSX). The runtime harness sets `NODE_PATH=REPO_ROOT/node_modules` so Node can resolve `react` + `react/jsx-runtime` from the harness's location under pytest's tmp directory. The harness exercises 8 end-to-end scenarios: every constant (TREE_WIDTH_STORAGE_KEY literal, MIN_TREE_WIDTH_PX=192, VIEWER_RESERVED_PX=320), 6 clampTreeWidth cases (happy path, below min, above max, too-narrow shell with MAX clamping to MIN, exactly at MIN, exactly at MAX), 4 readSavedTreeWidth cases (happy path, absent key, getItem throws + swallow, localStorage undefined), 3 writeSavedTreeWidth cases (happy path persisting under TREE_WIDTH_STORAGE_KEY, setItem throws + swallow, localStorage undefined + no-op), 3 clearSavedTreeWidth cases (happy path removing TREE_WIDTH_STORAGE_KEY, removeItem throws + swallow, localStorage undefined + no-op), and the round-trip write + read + clear lifecycle.
      - `tests/test_research_styles.py` — extension. Adds `.fex-splitter` to `FEX_EXPLORER_BASES`. Adds a focused `W6_3_SPLITTER_SELECTORS` parametrize that pins the splitter cascade (`.fex-splitter`, `.fex-splitter::after`, `.fex-splitter:hover`, `.fex-splitter.dragging`, `.fex-splitter:focus-visible`) under `@layer components` and forbids the same selectors under `@layer base` (the W6.2 chain-topology contract is preserved).
  - The W6.3 work honours the isolation constraints verbatim:
      - `/explorer/page.tsx` is untouched (the existing W6.1 route stays unchanged).
      - Legacy `web/` directory is untouched; FastAPI is untouched; the static-export pipeline is untouched; the Next.js plugin surface is untouched (the W6.3 Splitter uses plain React + the DOM API only — no `<Script>`, no CDN-script loader, no new dependency).
      - The single raw localStorage key `taxa.fex.treeWidth` is owned by the Splitter — no `@taxa/browser-state` import, no settings reset, no domain state mutation, no materialization, no server-side surface. The user-authorized W6.3 decision keeps the storage key OUT of `@taxa/browser-state` (the design.md §"Key boundaries" rationale: raw `taxa.fex.treeWidth` stays local to Explorer to avoid browser-state chunk scope expansion; the spec already excludes this key from the browser-state hydration surface — see `openspec/changes/complete-taxa-frontend-migration/specs/browser-state-hydration/spec.md` line 180: `taxa.fex.treeWidth key (used by the splitter) is **out of** [scope of browser-state]`).
      - No commit, push, PR, or work outside the declared edit surfaces.
  - Test policy correction (user-authorized W6.3 carveout): `tests/test_browser_state_keys.py::test_other_module_does_not_touch_localstorage[research]` now carves out the single documented exception — `src/modules/research/presentation/Splitter.tsx`'s `taxa.fex.treeWidth` raw localStorage key — while keeping the prohibition in force for every other Research source and every other capability module (`taxonomy`, `app-shell`, `design-system`). The carveout mirrors the OpenSpec boundary at `openspec/changes/complete-taxa-frontend-migration/specs/browser-state-hydration/spec.md` Notes: `taxa.fex.treeWidth key (used by the splitter) is out of scope for browser-state`. Every other Research source must still stay free of raw `localStorage.getItem` / `setItem` / `removeItem` references; every Splitter.tsx `localStorage.{getItem,setItem,removeItem}` reference must live inside a gated call expression `(globalThis|window).localStorage.{getItem,setItem,removeItem}(TREE_WIDTH_STORAGE_KEY, ...)` — a bare `localStorage.X` reference (no `globalThis`/`window` gate), a different key, or a non-gated prefix would still trip the assertion. The `@taxa/browser-state` typed-store surface, the four-typed-key contract, and every other source-level purity guard stay unchanged; only the cross-module localStorage-policy test is narrowed to the documented exception.
  - Evidence (this work unit): 155/155 focused W6.1 + W6.2 Research tests remain green (49 W6.1 mount + 30 styles + 76 W1–W4b4 regression); 11 new focused W6.3 splitter tests + 1 compile fixture + 1 runtime harness + 1 project-wide typecheck are green; the 216-test W6.1 + W6.2 + W6.3 focused Research regression group is green (100 explorer mount + 40 styles + 76 W1–W4b4 cross-module); the strict isolated TypeScript compile under `--lib ES2022,DOM --jsx react-jsx` (the W6.3 DOM-bound Splitter contract) passes; the project-wide strict TypeScript compile (`tsc --noEmit --project tsconfig.json`) passes; the alphabetic + chain-topology + allow-list tests for the `.fex-*` cascade are green with `.fex-splitter` whitelisted. Independent verification pending — the parent must run the focused pytest group + the strict typecheck independently before this evidence flips to "verified". The user explicitly accepted the W6.3 documented size exception (1 new file + 1 barrel extension + 5 CSS rules + 12 new tests + 1 runtime harness + tracker evidence ≈ ~700 lines): splitter drag handle, its pure helpers + storage constants, its barrel re-export, its CSS cascade, its source-level purity guard, its compile + runtime harness, its ARIA + title literal guard, its Explorer-mounts-splitter order guard, and its tracker evidence are cohesive because splitting would separate the splitter behavior from its pure-storage proof + its runtime proof. Work-unit commit: pending user authorization. The ODD-MIGRATE-003 task bullet stays open because W6.4+ materialization / open-folder / each preview family remain. The W6.3 splitter is reviewable in isolation; the `web/file_explorer.js` legacy splitter oracle is mirrored byte-for-byte so a future UX iteration can A/B compare by side-loading the W6.3 route against the legacy Browser tab without functional surprises.
  - Progress: W64A-JSON-001 + W64B-DOCX-002 (W6.4a typed `cdn-failed` viewer materialization) are implemented by this work unit. The W6.4a slice closes the W6 React preview-materialization set: W64A materializes the native JSON Tree viewer (the spec's "Tree viewer tab / No CDN is used." requirement) + W64B materializes the DOCX viewer via Next 16's `<Script>` loader + the legacy-pinned mammoth CDN. The W6.4a slice preserves every W6.1 / W6.2 / W6.3 + W1–W4b4 pin while extending the public `ViewerDispatch` union with a typed `cdn-failed` recovery literal on every CDN-dependent family (DOCX / Spreadsheet / EPUB / CSV) — the bytes-missing offline reason stays the only offline literal for the native families (TXT / MD / HTML / IMG / VIDEO / SVG / JSON). The W6.4a contract adds:
      - `src/modules/research/application/renderers.ts` — W64B typed union extension. Each `*-offline` `ViewerDispatch` variant (`docx-offline` + `sheet-offline` + `epub-offline` + `table-offline`) extends its `reason` field from the pre-W64B literal `"bytes-missing"` to the typed union `"bytes-missing" | "cdn-failed"`. `json-offline.reason` keeps the pre-W64B `"bytes-missing"` literal (JSON is native + CDN-free; there is no CDN to fail). The W4b1 / W4b2 / W4b3 / W4b4 application-layer dispatches still emit `"bytes-missing"` (the dispatcher can only detect the bytes-missing path at dispatch time — the mount owns the `"cdn-failed"` recovery path). The JSDoc comments on each `*-offline` variant + the `json-offline` exception are updated to surface the W64B typed union rationale.
      - `src/modules/research/presentation/explorer-state.ts` — W64B `bytesRequiredForFormat` flip. The `bytesRequiredForFormat("docx")` matrix entry flips from `false` to `true` so the existing Viewer.tsx bytes-fetch effect reads DOCX bytes through the same seam the TXT / MD / SVG / JSON effects already use. Mirrors the W64A JSON flip — the matrix stays honest end-to-end. Every other literal is unchanged (XLS / XLSX / EPUB / CSV / TSV stay `false` because those CDN families are NOT yet wired in the W6.4b Viewer — only DOCX materializes in this slice; their typed union contract lands on the `*-offline` variants but their bytes-required lifecycle lands as a separately authorized later slice).
      - `src/modules/research/presentation/Viewer.tsx` — W64B DOCX materialization + W64A JSON Tree viewer materialization. The W6.4a mount materializes:
          * **W64A-JSON-001**: the typed `json-source` dispatch routes through `renderJsonTree` (the dedicated `JsonNode` + `JsonContainer` recursive renderer + the legacy `MAX_JSON_NODES = 50_000` truncation cap + the legacy `[root]` synthetic root key + the `Tree truncated — open raw` banner — pinned byte-for-byte against the legacy `web/file_viewer.js::renderJsonTree` oracle). The pure `parseJsonTree` + `MAX_JSON_NODES` helpers stay module-local to `explorer-state.ts` (the public barrel is intentionally NOT extended for the W64A surface — verified by `test_w64a_barrel_does_not_reexport_w64a_helpers`). The `json-offline` dispatch stays in the W6.1 cdn-pending catch-all so the existing missing-bytes fallback/download behavior is preserved verbatim (verified by `test_w64a_json_viewer_renders_json_offline_via_existing_fallback`). The JSON branch is XSS-safe by React's text-content escaping (the `[root]` + key + leaf values flow through React children — NOT `dangerouslySetInnerHTML`, verified by `test_w64a_json_viewer_does_not_use_dangerously_set_inner_html`). The disclosure widget is accessible: `role="button"` + `tabIndex={0}` + Enter/Space keyboard handler + `aria-expanded` (the W3C ARIA disclosure-widget pattern; mirrors the legacy `web/file_viewer.js::renderJsonNode` shape verbatim).
          * **W64B-DOCX-002**: the typed `docx-source` dispatch routes through a dedicated `DocxRender` sub-component. The sub-component uses Next 16's `<Script src={dispatch.scriptUrl} strategy="afterInteractive" onLoad={handleScriptLoad} onError={handleScriptError}>` component (see `node_modules/next/dist/docs/01-app/03-api-reference/02-components/script.md`) as the loader surface — the loader passes the `MAMMOTH_CDN_URL` + `MAMMOTH_GLOBAL_NAME = "mammoth"` constants from the W4b1 dispatch contract verbatim (a future PR that bumps the CDN pin lands in lock-step across the dispatcher constant + the loader site). On script load the mount reaches the pinned mammoth global through `window[dispatch.scriptGlobal].convertToHtml({arrayBuffer: dispatch.bytes.buffer as ArrayBuffer})` (mirrors the legacy `web/file_viewer.js::renderDocx` shape verbatim — mammoth already strips `<script>` + event handlers per `design.md` §8, so the React `dangerouslySetInnerHTML` injection is the same XSS-safe shape as the legacy `Range.createContextualFragment` call site). The mount maintains its own `loading` / `loaded` / `error` state — on `Script.onError` OR `mammoth.convertToHtml(...)` rejection OR thrown exception the mount flips to a typed `cdn-failed` recovery state that's distinct from the `bytes-missing` offline path the dispatcher emits at dispatch time. The recovery state synthesizes a fresh `docx-offline` dispatch with `reason: "cdn-failed"` and routes through `renderOfflineCard` so the existing download affordance stays in place while the typed `reason` literal is first-class (the W64B typed union `"bytes-missing" | "cdn-failed"` on the `docx-offline` variant lets the surface distinguish the two failure sources). The `docx-offline` (the W6.1 bytes-missing path the dispatcher emits) stays in the cdn-pending catch-all so the existing bytes-missing fallback/download behavior is preserved verbatim.
      - The W6.4a work honours the isolation constraints verbatim:
          - `/explorer/page.tsx` is untouched (the existing W6.1 route stays unchanged).
          - Legacy `web/` directory is untouched; FastAPI is untouched; the static-export pipeline is untouched.
          - The Spreadsheet / EPUB / CSV / TSV source variants stay in the W6.1 cdn-pending catch-all (those CDN libraries land as separately authorized later slices; the W64B typed union is the common contract they'll surface through).
          - No new dependencies added (`package.json` untouched).
          - No commit, push, PR, or work outside the declared edit surfaces.
  - `tests/test_research_renderers.py` — extension. Adds 5 new focused tests: `test_w64b_renderers_docx_offline_reason_union_includes_cdn_failed` (pins the `docx-offline.reason` typed union `"bytes-missing" | "cdn-failed"` via the helper that brace-walks the variant block + verifies both literals appear in the `reason` field), `test_w64b_renderers_sheet_offline_reason_union_includes_cdn_failed` (mirrors the docx check for sheet-offline), `test_w64b_renderers_epub_offline_reason_union_includes_cdn_failed` (mirrors for epub-offline), `test_w64b_renderers_table_offline_reason_union_includes_cdn_failed` (mirrors for table-offline), `test_w64b_renderers_json_offline_reason_stays_bytes_missing_literal` (the JSON preservation guard — `json-offline.reason` is the pre-W64B literal, NOT the W64B union, because JSON is native + CDN-free). Each test parses `renderers.ts` after comments are stripped so author-friendly JSDoc references to forbidden-token words (e.g. "cdn-failed" in a comment explaining the rationale) don't trip the brace-walking matcher.
  - `tests/test_research_explorer_mount.py` — extension. Adds 6 new focused tests: `test_w64b_kernel_bytes_required_for_format_docx_is_true` (Node runtime harness + strict `true` assertion + the matrix-stays-honest assertion for every other `FileFormat` literal via the `compiled_w6_1_kernel` fixture), `test_w64b_viewer_imports_next_script_component` (source-level check that Viewer.tsx imports `Script` from `next/script` as a default import), `test_w64b_viewer_docx_source_uses_script_loader` (asserts the explicit `case "docx-source":` branch hands off to a dedicated `DocxRender` sub-component + the `DocxRender` function body renders a `<Script>` JSX element with the `onLoad` + `onError` handlers), `test_w64b_viewer_docx_source_calls_mammoth_convert_to_html` (asserts Viewer.tsx references `convertToHtml(...)` + reaches the global through `window[dispatch.scriptGlobal]` — NOT a hardcoded `"mammoth"` literal — + passes the bytes via the `arrayBuffer` wrapper), `test_w64b_viewer_docx_source_emits_cdn_failed_recovery_state` (asserts the typed `"cdn-failed"` literal appears in Viewer.tsx as the mount-emitted recovery state's `reason` field), `test_w64b_viewer_preserves_json_tree_pins` (the W64A preservation guard — verifies the JSON `case "json-source":` branch + the `[root]` literal + the `Tree truncated — open raw` banner text stay intact through the W64B Viewer.tsx edits). The W6.1 runtime harness step 3 (`bytesRequiredForFormat("docx")`) is updated from `false` to `true` with a clear W64B rationale message so the W6.1 contract stays honest end-to-end.
  - Evidence (this work unit): 11 new W64A + W64B focused tests green (2 W64A preservation guards from W64A + 5 renderers W64B + 6 explorer-mount W64B = 11 NEW focused tests); 250/250 focused W6.4a + W6.1 + W6.2 + W6.3 + W1–W4b4 Research tests remain green (147 explorer mount + 30 renderers + 30 styles + 20 domain + 16 application + 15 infra + 30 W6.4a additions = the W6.4a work stays inside the existing 250-test focused envelope); the project-wide strict TypeScript compile (`tsc --noEmit --project tsconfig.json`) is green; the strict isolated TypeScript compile for the W6.4a kernel + W4a–W4b4 renderers under `--lib ES2022` only (no DOM, no framework types) is green; the `node .pi/skills/impeccable/scripts/detect.mjs --json <changed UI targets>` detector pass returns empty findings (no pre-existing findings, no W64B-introduced findings). The W64A tests from the prior work-unit commit (`W64A-JSON-001`) remain green; the W64B runtime harness compiles the same `compiled_w6_1_kernel` fixture as the W6.1 + W64A harnesses, proving the bytes-required matrix is honest end-to-end. Work-unit commit: pending user authorization (the parent owns the local commit authorization per the W6.4a delivery strategy + the work-unit policy at the bottom of this tracker). The ODD-MIGRATE-003 task bullet stays open because W6.5+ preview families (XLS / EPUB / CSV / TSV via `<Script>` materialization) + the Explorer refresh bridge + the atomic cutover remain. The W6.4a mount materializes native JSON + DOCX/Mammoth in isolation; the Spreadsheet / EPUB / CSV / TSV cdn-pending catch-all stays in place for the W6.4a delivery boundary, and each preview family surfaces through the same `renderOfflineCard` shape with a typed `reason` literal that distinguishes the mount-detected `cdn-failed` path from the dispatcher-emitted `bytes-missing` path.
  - Progress: W64C-XLS-003 XLS / XLSX viewer materialization is implemented by this work unit. The W64C slice closes the W6.4c React preview-materialization set by materializing both XLS and XLSX through Next 16's `<Script>` loader + the legacy-pinned SheetJS CDN pin (`SHEETJS_CDN_URL` + `SHEETJS_GLOBAL_NAME`). The W64C contract preserves every W6.1 / W6.2 / W6.3 + W1–W4b4 + W6.4a pin while extending the existing Viewer mount with a typed `cdn-failed` recovery state and a multi-sheet picker for workbooks where `wb.SheetNames.length > 1`. The W64C contract adds:
      - `src/modules/research/presentation/explorer-state.ts` — W64C `bytesRequiredForFormat` flip. The `bytesRequiredForFormat("xls")` and `bytesRequiredForFormat("xlsx")` matrix entries flip from `false` to `true` so the existing Viewer.tsx bytes-fetch effect reads XLS / XLSX bytes through the same seam the TXT / MD / SVG / JSON / DOCX effects already use (mirrors the W64A JSON flip + the W64B-DOCX-002 DOCX flip — the matrix stays honest end-to-end). Every other literal is unchanged (EPUB / CSV / TSV stay `false` because those CDN families are NOT yet wired in the W6.4 mount; their typed union contract lands on the `*-offline` variants but their bytes-required lifecycle lands as a separately authorized later slice).
      - `src/modules/research/presentation/Viewer.tsx` — W64C SheetJS materialization. The typed `sheet-source` dispatch routes through a dedicated `SheetRender` sub-component (OUT of the W6.1 cdn-pending catch-all, mirroring the W64B-DOCX-002 separation). The sub-component uses Next 16's `<Script src={dispatch.scriptUrl} strategy="afterInteractive" onLoad={handleScriptLoad} onError={handleScriptError}>` component (see `node_modules/next/dist/docs/01-app/03-api-reference/02-components/script.md`) as the loader surface — the loader passes the pinned `SHEETJS_CDN_URL` + `SHEETJS_GLOBAL_NAME = "XLSX"` constants from the W4b2 dispatch contract verbatim (a future PR that bumps the CDN pin lands in lock-step across the dispatcher constant + the loader site). On script load the mount reaches the pinned SheetJS global through `window[dispatch.scriptGlobal].read(bytes, {type: "array"})` + `window[dispatch.scriptGlobal].utils.sheet_to_html(activeSheet)` (mirrors the legacy `web/file_viewer.js::renderSheet` shape verbatim — the legacy uses `loadScriptOnce("XLSX")` to inject the CDN, then `window.XLSX.read(data, { type: "array" })` to parse the workbook, then `window.XLSX.utils.sheet_to_html(sheet)` to emit the HTML table). SheetJS already emits a plain HTML `<table>` markup without `<script>` or event handlers per `design.md` §8, so the React mount's `dangerouslySetInnerHTML` injection is the same XSS-safe shape as the legacy `Range.createContextualFragment` call site. The mount maintains its own `loading` / `loaded` / `error` state — on `Script.onError` OR `SheetJS.read(...)` OR `utils.sheet_to_html(...)` exception the mount flips to a typed `cdn-failed` recovery state that's distinct from the `bytes-missing` offline path the dispatcher emits at dispatch time. The recovery state synthesizes a fresh `sheet-offline` dispatch with `reason: "cdn-failed"` and routes through `renderOfflineCard` so the existing download affordance stays in place while the typed `reason` literal is first-class (the W64B-DOCX-002 typed union `reason: "bytes-missing" | "cdn-failed"` on the `sheet-offline` variant — extended on `renderers.ts` — lets the surface distinguish the two failure sources). The `sheet-offline` (the W6.1 bytes-missing path the dispatcher emits) stays in the cdn-pending catch-all so the existing bytes-missing fallback/download behavior is preserved verbatim. When `wb.SheetNames.length > 1` the mount surfaces a `<select>` picker that switches the active sheet (mirrors the legacy `web/file_viewer.js::renderSheet` `sheetNames.length > 1` shape verbatim) — each `<option>` carries one sheet name from `wb.SheetNames` and the picker reuses the existing `.fex-snippet-btn` styling for its `<select>` element (matches the legacy `el("select", { class: "fex-snippet-btn font-mono-data text-mono-data text-on-surface bg-surface" })` shape verbatim). Both XLS and XLSX share the same SheetJS path; the dispatch carries `format` verbatim so the mount can branch on XLS vs XLSX for any future format-specific affordance.
      - `src/app/globals.css` — W64C XLS / XLSX SheetJS cascade. Adds the four `.fex-sheet-*` selectors under `@layer components` between `.fex-search-toggles` and `.fex-shell` in the alphabetic chain (the alphabetic contract requires `.fex-sheet-*` BEFORE `.fex-shell` because `.fex-sheet-*` 8th-char `e` (0x65) sorts BEFORE `.fex-shell` 8th-char `l` (0x6C)): `.fex-sheet-host` (vertical flex with picker gap), `.fex-sheet-picker` (horizontal flex for label + `<select>`), `.fex-sheet-picker-label` (text-body-sm + text-on-surface-variant typography — mirrors the legacy `el("label", { class: "text-body-sm text-on-surface-variant" }, "Sheet:")` shape), `.fex-sheet-table-host` (overflow-auto container — mirrors the legacy `el("div", { class: "overflow-auto" })` shape so wide sheets scroll horizontally without breaking the two-pane layout). Every selector is whitelisted in `FEX_EXPLORER_BASES` so the chain-topology + alphabetic guards stay green. The cascade contract respects the chain-topology rule — the `.fex-sheet-*` family mirrors the PR 3c-b alphabetic + chain-topology conventions (base → descendants → different base → period modifier → attribute modifier).
      - The W64C work honours the isolation constraints verbatim:
          - `/explorer/page.tsx` is untouched (the existing W6.1 route stays unchanged).
          - Legacy `web/` directory is untouched; FastAPI is untouched; the static-export pipeline is untouched.
          - EPUB / CSV / TSV source variants stay in the W6.1 cdn-pending catch-all (those CDN libraries land as separately authorized later slices; the W64B typed union `reason: "bytes-missing" | "cdn-failed"` is the common contract they'll surface through).
          - No new dependencies added (`package.json` untouched).
          - No commit, push, PR, or work outside the declared edit surfaces.
  - `tests/test_research_explorer_mount.py` extends the W6.4a surface with 7 new focused W64C tests + extends the W6.1 + W64B runtime harnesses with the W64C matrix flip: `test_w64c_kernel_bytes_required_for_format_xls_is_true` (the runtime harness compiles the `compiled_w6_1_kernel` fixture + asserts `bytesRequiredForFormat("xls") === true`, `bytesRequiredForFormat("xlsx") === true`, and the matrix stays honest for every other literal — TXT / MD / SVG / JSON / DOCX / PDF / HTML / HTM / JPG / CSV / TSV / EPUB / MP4 / other); `test_w64c_viewer_sheet_source_uses_script_loader` (asserts the explicit `case "sheet-source":` branch hands off to a dedicated `SheetRender` sub-component + the `SheetRender` function body renders a `<Script>` JSX element with `onLoad` + `onError` handlers); `test_w64c_viewer_sheet_source_calls_sheetjs_read_with_array_type` (asserts the onLoad handler reaches the pinned SheetJS global through `window[dispatch.scriptGlobal].read(bytes, {type: "array"})` — NOT a hardcoded `"XLSX"` literal — so a future PR that bumps the CDN pin lands in lock-step across the dispatcher constant + the loader site); `test_w64c_viewer_sheet_source_calls_sheet_to_html` (asserts the onLoad handler calls `window[dispatch.scriptGlobal].utils.sheet_to_html(activeSheet)` — the `utils` namespace is required because SheetJS exposes `sheet_to_html` under the pinned global's `utils` namespace); `test_w64c_viewer_sheet_source_renders_picker_for_multi_sheet` (asserts the SheetRender sub-component renders a `<select>` JSX element with one `<option>` per `sheetNames` entry + guards the picker on `sheetNames.length > 1`); `test_w64c_viewer_sheet_source_emits_cdn_failed_recovery_state` (asserts the literal `"cdn-failed"` recovery state + the synthesized `sheet-offline` dispatch in the SheetRender function body); `test_w64c_viewer_preserves_json_docx_sheet_offline_pins` (the W6.4a preservation guard — verifies the W64A-JSON-001 `case "json-source":` branch + the `[root]` literal + the `Tree truncated — open raw` banner text + the W64B-DOCX-002 `case "docx-source":` branch + the `DocxRender` sub-component + the `convertToHtml` call + the `arrayBuffer` wrapper all stay intact + the W6.1 cdn-pending catch-all still lists `docx-offline` + `sheet-offline` + `epub-offline` + `table-offline` + `json-offline` + `epub-source` + `table-source` for the bytes-missing offline paths + the catch-all no longer lists `sheet-source` because the W64C slice extracted it into the dedicated SheetRender sub-component). The W6.1 runtime harness + the W64B runtime harness are extended with the W64C matrix flip — both harnesses now assert `bytesRequiredForFormat("xls") === true` and `bytesRequiredForFormat("xlsx") === true` (the same pattern the W64B-DOCX-002 work unit used for the `docx === true` flip).
  - `tests/test_research_styles.py` extends the W6.2 + W6.3 cascade with the W64C surface. Adds `.fex-sheet-host`, `.fex-sheet-picker`, `.fex-sheet-picker-label`, `.fex-sheet-table-host` to the `FEX_EXPLORER_BASES` whitelist (the chain-topology guard) + adds the `W64C_SHEET_SELECTORS` tuple + two parametrized tests: `test_layer_components_declares_every_w64c_sheet_selector` (asserts every `.fex-sheet-*` selector resolves to a non-empty block under `@layer components`) + `test_layer_base_does_not_own_w64c_sheet_selectors` (asserts every `.fex-sheet-*` selector is FORBIDDEN under `@layer base` — the W6.4b React-mount surface contract). The alphabetic + chain-topology + allow-list tests for the `.fex-*` cascade stay green with `.fex-sheet-*` whitelisted in the right alphabetic position (before `.fex-shell`).
  - Evidence (this work unit): 7 new W64C-XLS-003 focused tests green (1 W64C matrix-flip harness + 6 W64C viewer source-level checks); the existing W6.1 + W64B runtime harness matrix updates also green; 253/253 focused W6.4 + W6.1 + W6.2 + W6.3 + W1–W4b4 Research tests remain green (124 explorer mount + 30 renderers + 48 styles + 20 domain + 16 application + 15 infra = 253 = 238 pre-W64C baseline + 15 W6.4c additions: 7 new W64C explorer-mount tests + 8 new W64C styles tests (2 parametrized test functions × 4 `.fex-sheet-*` selectors)); the project-wide strict TypeScript compile (`tsc --noEmit --project tsconfig.json`) is green; the strict isolated TypeScript compile for the W6.4 kernel + W4a–W4b4 renderers under `--lib ES2022` only (no DOM, no framework types) is green; the `node .pi/skills/impeccable/scripts/detect.mjs --json <changed UI targets>` detector pass returns two pre-existing findings on lines 2466 + 2474 of `src/app/globals.css` (the `.tree-row.focused` and `.tree-row.selected` `border-left: 3px solid` rules — both pre-existing from the W6.2 search surface, NOT introduced by W64C). The W64A + W64B tests from the prior work-unit commits remain green; the W6.1 + W64B runtime harnesses compile the same `compiled_w6_1_kernel` fixture as the W6.1 + W64A + W64B harnesses, proving the bytes-required matrix stays honest end-to-end across every W6.4c flip. Work-unit commit: pending user authorization (the parent owns the local commit authorization per the W6.4c delivery strategy + the work-unit policy at the bottom of this tracker). The ODD-MIGRATE-003 task bullet stays open because W6.4d EPUB / W6.4e CSV-TSV-via-Papa-Parse + the Explorer refresh bridge + the atomic cutover remain. The W6.4c mount materializes native JSON + DOCX/Mammoth + XLS/XLSX-SheetJS in isolation; the EPUB / CSV / TSV cdn-pending catch-all stays in place for the W6.4c delivery boundary, and each preview family surfaces through the same `renderOfflineCard` shape with a typed `reason` literal that distinguishes the mount-detected `cdn-failed` path from the dispatcher-emitted `bytes-missing` path.
  - Progress: W64D-EPUB-004 EPUB viewer materialization is implemented by this work unit. The W64D slice completes the EPUB materialization through Next 16's `<Script>` loader + the legacy-pinned epubjs CDN (`EPUBJS_CDN_URL` + `EPUBJS_GLOBAL_NAME = "ePub"` — the case-sensitive UMD global, lowercase `e` + capital `P`). The W64D contract preserves every W6.1 / W6.2 / W6.3 + W1–W4b4 + W6.4a + W6.4c pin while extending the existing Viewer mount with a typed `cdn-failed` recovery state, prev / next navigation handlers, and a module-scoped `previousBook` teardown lifecycle (mirrors the legacy `web/file_viewer.js::renderEpub` lines 429–438 `_currentBook.destroy()` lifecycle verbatim — the legacy tears down the previous book before mounting the new one so listeners don't leak per `design.md` §8). The W64D contract adds:
      - `src/modules/research/presentation/explorer-state.ts` — W64D `bytesRequiredForFormat` flip. The `bytesRequiredForFormat("epub")` matrix entry flips from `false` to `true` so the existing Viewer.tsx bytes-fetch effect reads EPUB bytes through the same seam the TXT / MD / SVG / JSON / DOCX / XLS / XLSX effects already use (mirrors the W64A JSON + W64B DOCX + W64C XLS / XLSX flips — the matrix stays honest end-to-end). Every other literal is unchanged (CSV / TSV stay `false` because Papa CDN materialization lands as a separately authorized later slice).
      - `src/modules/research/presentation/Viewer.tsx` — W64D EPUB materialization. The typed `epub-source` dispatch routes through a dedicated `EpubRender` sub-component (OUT of the W6.1 cdn-pending catch-all, mirroring the W64B-DOCX-002 + W64C-XLS-003 separation). The sub-component uses Next 16's `<Script src={dispatch.scriptUrl} strategy="afterInteractive" onLoad={handleScriptLoad} onError={handleScriptError}>` component (see `node_modules/next/dist/docs/01-app/03-api-reference/02-components/script.md`) as the loader surface — the loader passes the pinned `EPUBJS_CDN_URL` + `EPUBJS_GLOBAL_NAME = "ePub"` constants from the W4b3 dispatch contract verbatim (a future PR that bumps the CDN pin lands in lock-step across the dispatcher constant + the loader site). On script load the mount reaches the pinned epubjs global through `window[dispatch.scriptGlobal](dispatch.bytes.buffer)` (NOT a constructor with `new` — the UMD global IS a function; a constructor with `new` would throw at runtime), then calls `book.renderTo(hostEl, { width: "100%", height: "100%" })` to mount the EPUB (mirrors the legacy `web/file_viewer.js::renderEpub` `book.renderTo(epubHost, { width: "100%", height: "100%" })` shape verbatim). The mount owns a module-scoped `previousBook` reference (the legacy uses `_currentBook`; the React mount mirrors the same shape with a module-scoped `let previousBook:` declaration so the handle outlives the per-render closure) and tears down the previous book BEFORE rendering the new one so listeners don't leak per `design.md` §8 EPUB render lifecycle. The same cleanup runs on unmount AND on any change of the EPUB dispatch (the `useEffect` cleanup path preserves the React-mount equivalence of the legacy's "tear down on the NEXT open" shape). The mount surfaces prev / next click handlers that call `book.prev()` / `book.next()` (mirrors the legacy `gotoPrev.addEventListener("click", () => book.prev())` + `gotoNext.addEventListener("click", () => book.next())` shape verbatim). The mount maintains its own `loading` / `loaded` / `error` state — on `Script.onError` OR `ePub(arrayBuffer)` construction exception OR `book.renderTo(...)` throw OR `book.prev()` / `book.next()` exception the mount flips to a typed `cdn-failed` recovery state that's distinct from the `bytes-missing` offline path the dispatcher emits at dispatch time. The recovery state synthesizes a fresh `epub-offline` dispatch with `reason: "cdn-failed"` and routes through `renderOfflineCard` so the existing download affordance stays in place while the typed `reason` literal is first-class (the W64B-DOCX-002 typed union `reason: "bytes-missing" | "cdn-failed"` on the `epub-offline` variant — extended on `renderers.ts` — lets the surface distinguish the two failure sources). The `epub-offline` (the W6.1 bytes-missing path the dispatcher emits) stays in the cdn-pending catch-all so the existing bytes-missing fallback/download behavior is preserved verbatim.
      - `src/app/globals.css` — W64D EPUB viewer cascade. Adds the three `.fex-epub-*` selectors under `@layer components` between `.fex-empty-state .fex-empty-state-icon` and `.fex-image` in the alphabetic chain (the alphabetic contract requires `.fex-epub-*` AFTER `.fex-empty-state` — `m` (0x6D) of `empty` < `p` (0x70) of `epub` — and BEFORE `.fex-image` — `e` < `i`): `.fex-epub-frame` (the `book.renderTo` host with the `min-h-[480px]` floor + surface background + outline-variant border + border-radius so the paged book has a stable target to render into regardless of viewport — mirrors the legacy `el("div", { class: "flex-1 min-h-[480px]" })` shape verbatim, with the `min-h-[480px]` lifted into the cascade so the React mount owns the floor), `.fex-epub-host` (vertical flex with the nav-row gap so the prev/next buttons can sit below the book), `.fex-epub-nav` (the nav row wrapper that carries the gap + layout). Every selector is whitelisted in `FEX_EXPLORER_BASES` so the chain-topology + alphabetic guards stay green. The cascade contract respects the chain-topology rule — the `.fex-epub-*` family mirrors the PR 3c-b alphabetic + chain-topology conventions (base → descendants → different base → period modifier → attribute modifier).
      - The W64D work honours the isolation constraints verbatim:
          - EPUB viewer materialization is isolated to `Viewer.tsx` + `explorer-state.ts` + `globals.css` — no FastAPI / API / static-export changes.
          - JSON Tree viewer (W64A-JSON-001) — untouched. DOCX mount (W64B-DOCX-002) — untouched. SheetJS mount (W64C-XLS-003) — untouched. The pre-W64D `epub-offline` (bytes-missing) dispatch stays in the W6.1 cdn-pending catch-all so the existing W6.1 download-link affordance is preserved verbatim. The CSV / TSV / JSON source variants stay in the W6.1 cdn-pending catch-all (W64D is EPUB-only materialization; Papa CDN lands as a separately authorized later slice).
          - No new dependencies added (`package.json` untouched).
  - `tests/test_research_explorer_mount.py` extends the W64C surface with 8 new focused W64D tests + extends the W6.1 + W64B + W64C runtime harnesses with the W64D matrix flip: `test_w64d_kernel_bytes_required_for_format_epub_is_true` (the runtime harness compiles the `compiled_w6_1_kernel` fixture + asserts `bytesRequiredForFormat("epub") === true`, and the matrix stays honest for every other literal — TXT / MD / SVG / JSON / DOCX / XLS / XLSX / PDF / HTML / HTM / JPG / CSV / TSV / MP4 / other); `test_w64d_viewer_epub_source_uses_script_loader` (asserts the explicit `case "epub-source":` branch hands off to a dedicated `EpubRender` sub-component + the `EpubRender` function body renders a `<Script>` JSX element with `onLoad` + `onError` handlers); `test_w64d_viewer_epub_source_constructs_book_via_window_call` (asserts the onLoad handler reaches the pinned epubjs global through `window[dispatch.scriptGlobal](dispatch.bytes.buffer)` — NOT a hardcoded `"ePub"` literal — so a future PR that bumps the CDN pin lands in lock-step across the dispatcher constant + the loader site, AND that Viewer.tsx MUST NOT call a constructor with `new` because the UMD global is a function); `test_w64d_viewer_epub_source_calls_render_to` (asserts the onLoad handler calls `book.renderTo(hostEl, ...)` to mount the EPUB into the React tree — mirrors the legacy `web/file_viewer.js::renderEpub` shape verbatim); `test_w64d_viewer_epub_source_surfaces_prev_next_navigation` (asserts prev / next click handlers call `book.prev()` / `book.next()` — mirrors the legacy `gotoPrev` / `gotoNext` shape verbatim); `test_w64d_viewer_epub_source_owns_previous_book_lifecycle` (asserts the `EpubRender` sub-component owns the module-scoped `previousBook` reference + the `.destroy()` teardown + the `useEffect` cleanup wiring — mirrors the legacy `_currentBook.destroy()` lifecycle verbatim so listeners don't leak per `design.md` §8); `test_w64d_viewer_epub_source_emits_cdn_failed_recovery_state` (asserts the literal `"cdn-failed"` recovery state + the synthesized `epub-offline` dispatch in the EpubRender function body); `test_w64d_viewer_preserves_json_docx_sheet_epub_offline_pins` (the W6.4a + W6.4b + W6.4c preservation guard — verifies the W64A-JSON-001 `case "json-source":` branch + the `[root]` literal + the `Tree truncated — open raw` banner text + the W64B-DOCX-002 `case "docx-source":` branch + the `DocxRender` sub-component + the `convertToHtml` call + the `arrayBuffer` wrapper + the W64C-XLS-003 `case "sheet-source":` branch + the `SheetRender` sub-component + the `sheet_to_html` reference all stay intact + the W6.1 cdn-pending catch-all still lists `docx-offline` + `sheet-offline` + `epub-offline` (pre-W64D bytes-missing) + `table-offline` + `json-offline` + `table-source` for the bytes-missing offline paths + the catch-all no longer lists `epub-source` because the W64D slice extracted it into the dedicated `EpubRender` sub-component). The W6.1 + W64B + W64C runtime harnesses are extended with the W64D matrix flip — every harness now asserts `bytesRequiredForFormat("epub") === true` (the same pattern the W64B-DOCX-002 + W64C-XLS-003 work units used for the `docx === true` + `xls/xlsx === true` flips), and the W64C preservation test is updated to acknowledge that `epub-source` is no longer in the cdn-pending catch-all (the W64D slice extracted it; only `table-source` remains as a deferred family).
  - `tests/test_research_styles.py` extends the W6.4c cascade with the W64D surface. Adds `.fex-epub-host`, `.fex-epub-frame`, `.fex-epub-nav` to the `FEX_EXPLORER_BASES` whitelist (the chain-topology guard) + adds the `W64D_EPUB_SELECTORS` tuple + two parametrized tests: `test_layer_components_declares_every_w64d_epub_selector` (asserts every `.fex-epub-*` selector resolves to a non-empty block under `@layer components`) + `test_layer_base_does_not_own_w64d_epub_selectors` (asserts every `.fex-epub-*` selector is FORBIDDEN under `@layer base` — the W6.4b React-mount surface contract). The alphabetic + chain-topology + allow-list tests for the `.fex-*` cascade stay green with `.fex-epub-*` whitelisted in the right alphabetic position (AFTER `.fex-empty-state .fex-empty-state-icon` — `m` < `p` — and BEFORE `.fex-image` — `e` < `i`).
  - Evidence (this work unit): 8 new W64D-EPUB-004 focused tests green (1 W64D matrix-flip harness + 7 W64D viewer source-level checks); the existing W6.1 + W64B + W64C runtime harness matrix updates also green; 267/267 focused W6.4d + W6.4c + W6.4b + W6.4a + W6.1 + W6.2 + W6.3 + W1–W4b4 Research tests remain green (132 explorer mount + 30 renderers + 54 styles + 20 domain + 16 application + 15 infra = 267 = 253 pre-W64D baseline + 14 W6.4d additions: 8 new W64D explorer-mount tests + 6 new W64D styles tests (2 parametrized test functions × 3 `.fex-epub-*` selectors)); the project-wide strict TypeScript compile (`tsc --noEmit --project tsconfig.json`) is green; the strict isolated TypeScript compile for the W6.4 kernel + W4a–W4b4 renderers under `--lib ES2022` only (no DOM, no framework types) is green; the `node .pi/skills/impeccable/scripts/detect.mjs --json <changed UI targets>` detector pass returns two pre-existing findings on lines 2521 + 2529 of `src/app/globals.css` (the `.tree-row.focused` and `.tree-row.selected` `border-left: 3px solid` rules — both pre-existing from the W6.2 search surface, NOT introduced by W64D; the line numbers shifted because the new `.fex-epub-*` selectors added ~55 lines). The W64A + W64B + W64C tests from the prior work-unit commits remain green; the W6.1 + W64B + W64C runtime harnesses compile the same `compiled_w6_1_kernel` fixture as the W6.1 + W64A + W64B + W64C harnesses, proving the bytes-required matrix stays honest end-to-end across every W6.4d flip. Work-unit commit: pending user authorization (the parent owns the local commit authorization per the W6.4d delivery strategy + the work-unit policy at the bottom of this tracker). The ODD-MIGRATE-003 task bullet stays open because W6.4e CSV / TSV via Papa Parse + the Explorer refresh bridge + the atomic cutover remain. The W6.4d mount materializes native JSON + DOCX/Mammoth + XLS / XLSX-SheetJS + EPUB / epubjs in isolation; the CSV / TSV / JSON cdn-pending catch-all stays in place for the W6.4d delivery boundary, and each preview family surfaces through the same `renderOfflineCard` shape with a typed `reason` literal that distinguishes the mount-detected `cdn-failed` path from the dispatcher-emitted `bytes-missing` path.
  - Progress: W64E-CSV-005 CSV / TSV Table viewer materialization is implemented by this work unit. The W64E slice completes the CSV / TSV materialization through Next 16's `<Script>` loader + the legacy-pinned Papa Parse CDN (`PAPA_CDN_URL` + `PAPA_GLOBAL_NAME = "Papa"` — the literal UMD global). The W64E contract preserves every W6.1 / W6.2 / W6.3 + W1–W4b4 + W6.4a + W6.4c + W6.4d pin while extending the existing Viewer mount with a typed `cdn-failed` recovery state + UTF-8 decode + Papa `parse(text, {delimiter, skipEmptyLines: true})` lifecycle (mirrors the legacy `web/file_viewer.js::renderTable` lines 550–619 verbatim — first row is the header; a headerless file synthesises `Col N` labels; cell strings coerce to `""` when missing). The W64E contract adds:
      - `src/modules/research/presentation/explorer-state.ts` — W64E `bytesRequiredForFormat` flip. The `bytesRequiredForFormat("csv")` AND `bytesRequiredForFormat("tsv")` matrix entries flip from `false` to `true` so the existing Viewer.tsx bytes-fetch effect reads CSV / TSV bytes through the same seam the TXT / MD / SVG / JSON / DOCX / XLS / XLSX / EPUB effects already use (mirrors the W64A JSON + W64B DOCX + W64C XLS / XLSX + W64D EPUB flips — the matrix stays honest end-to-end). Every other literal is unchanged.
      - `src/modules/research/presentation/Viewer.tsx` — W64E Papa Parse materialization. The typed `table-source` dispatch routes through a dedicated `TableRender` sub-component (OUT of the W6.1 cdn-pending catch-all, mirroring the W64B-DOCX-002 + W64C-XLS-003 + W64D-EPUB-004 separation). The sub-component uses Next 16's `<Script src={dispatch.scriptUrl} strategy="afterInteractive" onLoad={handleScriptLoad} onError={handleScriptError}>` component (see `node_modules/next/dist/docs/01-app/03-api-reference/02-components/script.md`) as the loader surface — the loader passes the pinned `PAPA_CDN_URL` + `PAPA_GLOBAL_NAME = "Papa"` constants from the W4b4 dispatch contract verbatim (a future PR that bumps the CDN pin lands in lock-step across the dispatcher constant + the loader site). On script load the mount UTF-8-decodes the bytes through `new TextDecoder("utf-8", {fatal: false}).decode(dispatch.bytes)` (matches the W4a `text-pre` decoder shape — `fatal: false` so malformed UTF-8 yields U+FFFD rather than throwing) and calls `window[dispatch.scriptGlobal].parse(text, {delimiter, skipEmptyLines: true})` (mirrors the legacy `web/file_viewer.js::renderTable` `window.Papa.parse(text, {delimiter, skipEmptyLines: true})` shape verbatim). The mount passes the typed `delimiter` literal from the W4b4 dispatch (`","` for CSV, `"\t"` for TSV) verbatim — the `format`-derived fallback mirrors the legacy `renderTable` ternary `const delimiter = ext === "tsv" ? "\t" : ","` verbatim and stays honest for future consumer slices that may omit `delimiter`. The mount renders the parsed rows as a sticky `<thead>` + zebra `<tbody>` table inside the existing `.fex-csv-scroller` wrapper (the cascade is already shipped by the W6.1 migration — `.fex-csv-scroller { max-height: 100%; overflow: auto }` + `.fex-csv-table` (JetBrains Mono typography + outline-variant borders) + `.fex-csv-table thead th { position: sticky; top: 0; z-index: 1 }` (sticky header) + `.fex-csv-table tbody td` + `.fex-csv-table tbody tr:nth-child(even) td` (zebra) — the React mount just paints the right element shape + class names so the cascade applies). The first row is the header (Papa's default behaviour when `header` is omitted); a headerless file (every cell on row 0 is empty / whitespace) synthesises `Col N` labels; missing cells coerce to `""` (mirrors the legacy `String(r[i] ?? "")` shape verbatim). The mount maintains its own `loading` / `loaded` / `error` state — on `Script.onError` OR `Papa.parse(...)` exception OR non-benign Papa parse error (the mount filters the benign `TooFewFields` / `TooManyFields` warnings — Papa reports these for every trailing-delimiter row, mirroring the legacy `if (first.code !== "TooFewFields" && first.code !== "TooManyFields")` shape verbatim) the mount flips to a typed `"cdn-failed"` recovery state that's distinct from the `bytes-missing` offline path the dispatcher emits at dispatch time. The recovery state synthesizes a fresh `table-offline` dispatch with `reason: "cdn-failed"` and routes through `renderOfflineCard` so the existing download affordance stays in place while the typed `reason` literal is first-class (the W64B-DOCX-002 typed union `reason: "bytes-missing" | "cdn-failed"` on the `table-offline` variant — extended on `renderers.ts` — keeps both failure literals first-class). The `table-offline` (the W6.1 bytes-missing path the dispatcher emits) stays in the cdn-pending catch-all so the existing bytes-missing fallback/download behavior is preserved verbatim. The cell strings flow through React children (NOT `dangerouslySetInnerHTML`) so the XSS-safe shape is enforced by React's text-content escaping path (mirrors the legacy `el("td", null, cell)` cell-string escaping shape verbatim).
      - The W64E work honours the isolation constraints verbatim:
          - CSV / TSV viewer materialization is isolated to `Viewer.tsx` + `explorer-state.ts` — no FastAPI / API / static-export changes.
          - JSON Tree viewer (W64A-JSON-001) — untouched. DOCX mount (W64B-DOCX-002) — untouched. SheetJS mount (W64C-XLS-003) — untouched. EPUB mount (W64D-EPUB-004) — untouched. The pre-W64E `table-offline` (bytes-missing) dispatch stays in the W6.1 cdn-pending catch-all so the existing W6.1 download-link affordance is preserved verbatim.
          - No new dependencies added (`package.json` untouched).
  - `tests/test_research_explorer_mount.py` extends the W64D surface with 6 new focused W64E tests + extends the W6.1 + W64B + W64C + W64D runtime harnesses with the W64E matrix flip: `test_w64e_kernel_bytes_required_for_format_csv_tsv_is_true` (the runtime harness compiles the `compiled_w6_1_kernel` fixture + asserts `bytesRequiredForFormat("csv") === true` + `bytesRequiredForFormat("tsv") === true`, and the matrix stays honest for every other literal — TXT / MD / SVG / JSON / DOCX / XLS / XLSX / EPUB / PDF / HTML / HTM / JPG / MP4 / other); `test_w64e_viewer_csv_tsv_narrow_behavior_proof` (the narrow CSV / TSV behavior proof test that asserts all four observable contract parts in one shot — both delimiters (CSV `","` + TSV `"\t"`) derived from the dispatch's `format` field when `dispatch.delimiter` is unavailable + sticky `<thead>` + zebra `<tbody>` + the `.fex-csv-scroller` wrapper + the `.fex-csv-table` selector + typed `cdn-failed` recovery via the synthesized `table-offline` dispatch); `test_w64e_viewer_table_source_uses_script_loader` (asserts the explicit `case "table-source":` branch hands off to a dedicated `TableRender` sub-component + the `TableRender` function body renders a `<Script>` JSX element with `onLoad` + `onError` handlers); `test_w64e_viewer_table_source_calls_papa_parse_with_skip_empty_lines` (asserts the onLoad handler reaches the pinned Papa Parse global through `window[dispatch.scriptGlobal].parse(...)` — NOT a hardcoded `"Papa"` literal — so a future PR that bumps the CDN pin lands in lock-step across the dispatcher constant + the loader site, AND that the call passes `{skipEmptyLines: true}` so empty rows don't pollute the rendered table — mirrors the legacy `Papa.parse(text, {delimiter, skipEmptyLines: true})` shape verbatim); `test_w64e_viewer_table_source_decodes_utf8` (asserts the onLoad handler UTF-8-decodes the bytes through `new TextDecoder("utf-8", {fatal: false})` so malformed UTF-8 sequences yield a U+FFFD replacement character rather than throwing — mirrors the W4a `text-pre` decoder shape); `test_w64e_viewer_preserves_json_docx_sheet_epub_offline_pins` (the W6.4a + W6.4b + W6.4c + W6.4d preservation guard — verifies the W64A-JSON-001 `case "json-source":` branch + the `[root]` literal + the `Tree truncated — open raw` banner text + the W64B-DOCX-002 `case "docx-source":` branch + the `DocxRender` sub-component + the `convertToHtml` call + the `arrayBuffer` wrapper + the W64C-XLS-003 `case "sheet-source":` branch + the `SheetRender` sub-component + the `sheet_to_html` reference + the W64D-EPUB-004 `case "epub-source":` branch + the `EpubRender` sub-component + the `renderTo` call all stay intact + the W6.1 cdn-pending catch-all still lists `docx-offline` + `sheet-offline` + `epub-offline` + `table-offline` + `json-offline` for the bytes-missing offline paths + the catch-all no longer lists `table-source` because the W64E slice extracted it into the dedicated `TableRender` sub-component). The W6.1 + W64B + W64C + W64D runtime harnesses are extended with the W64E matrix flip — every harness now asserts `bytesRequiredForFormat("csv") === true` AND `bytesRequiredForFormat("tsv") === true` (the same pattern the W64A-JSON-001 + W64B-DOCX-002 + W64C-XLS-003 + W64D-EPUB-004 work units used for the previous matrix flips), and the W64C + W64D preservation tests are updated to acknowledge that `table-source` is no longer in the cdn-pending catch-all (the W64E slice extracted it; only the `*-offline` bytes-missing variants remain). The narrow behavior proof covers both delimiters (CSV `","` + TSV `"\t"`) + sticky `<thead>` + zebra `<tbody>` + typed `cdn-failed` recovery in a single test, end-to-end.
  - Evidence (this work unit): 6 new W64E-CSV-005 focused tests green (1 W64E matrix-flip runtime harness + 1 narrow CSV / TSV behavior proof + 4 W64E viewer source-level checks); the existing W6.1 + W64B + W64C + W64D runtime harness matrix updates also green; 273/273 focused W6.4e + W6.4d + W6.4c + W6.4b + W6.4a + W6.1 + W6.2 + W6.3 + W1–W4b4 Research tests remain green (138 explorer mount + 30 renderers + 54 styles + 20 domain + 16 application + 15 infra = 273 = 267 pre-W64E baseline + 6 W6.4e additions: 6 new W64E explorer-mount tests); the project-wide strict TypeScript compile (`tsc --noEmit --project tsconfig.json`) is green; the strict isolated TypeScript compile for the W6.4 kernel + W4a–W4b4 renderers under `--lib ES2022` only (no DOM, no framework types) is green; the `node .pi/skills/impeccable/scripts/detect.mjs --json <changed UI targets>` detector pass returns two pre-existing findings on lines 2521 + 2529 of `src/app/globals.css` (the `.tree-row.focused` and `.tree-row.selected` `border-left: 3px solid` rules — both pre-existing from the W6.2 search surface, NOT introduced by W64E; the line numbers match the W64D baseline because W64E doesn't add any new CSS selectors — the `.fex-csv-scroller` + `.fex-csv-table` cascade was already shipped by the W6.1 migration). The W64A + W64B + W64C + W64D tests from the prior work-unit commits remain green; the W6.1 + W64B + W64C + W64D runtime harnesses compile the same `compiled_w6_1_kernel` fixture as the W6.1 + W64A + W64B + W64C + W64D harnesses, proving the bytes-required matrix stays honest end-to-end across every W6.4e flip. Work-unit commit: pending user authorization (the parent owns the local commit authorization per the W6.4e delivery strategy + the work-unit policy at the bottom of this tracker). The ODD-MIGRATE-003 task bullet stays open because the Explorer refresh bridge + the atomic cutover remain. The W6.4e mount materializes native JSON + DOCX/Mammoth + XLS / XLSX-SheetJS + EPUB / epubjs + CSV / TSV-Papa-Parse in isolation; the only deferred family is the Explorer refresh bridge (`CustomEvent`, new browser-state key, or router-key re-mount per a future architecture decision). The W6.4e mount emits each preview family through the same `renderOfflineCard` shape with a typed `reason` literal that distinguishes the mount-detected `cdn-failed` path from the dispatcher-emitted `bytes-missing` path — the pre-W64E `table-offline` (bytes-missing) dispatch stays in the W6.1 cdn-pending catch-all so the existing W6.1 download-link affordance is preserved verbatim.


- [ ] ODD-MIGRATE-004 Move AC-21 and remaining active consumers off `web/`.
  - Route: delegated worker.
  - Scope: search-engine catalog, consumer manifest, and affected tests.
  - Evidence: AC-21, smoke, and consumer-manifest tests pass against the React replacement path.

- [ ] ODD-MIGRATE-005 Produce fresh candidate evidence and select the cutover approach.
  - Route: delegated verification.
  - Scope: reproducible build inventory (G2), behavior/a11y parity (G4), candidate-baseline comparison (G5), and canonical dry-run rehearsal (G6).
  - Evidence: all artifacts are current, comparable, and pass their fail-closed thresholds. If any gate fails, stop before activation.

- [ ] ODD-MIGRATE-006 Perform the atomic production cutover.
  - Route: delegated worker after ODD-MIGRATE-005 passes and the user explicitly authorizes activation.
  - Scope: `make api` build path, `api/server.py::WEB_DIR`, all Tier-2 consumer updates, the selected build artifact, and legacy `web/` retirement in one release.
  - Evidence: a single rollback boundary and no active consumer pointed at a retired path.

- [ ] ODD-MIGRATE-007 Verify the activated runtime and rollback.
  - Route: delegated verifier.
  - Scope: full relevant tests, FastAPI-served smoke/browser/a11y validation, G3 Tier-2 readiness, G6 execution/rollback rehearsal, and a revert proof.
  - Evidence: CI-equivalent suite and the rollback restore the legacy behavior as one unit.

- [ ] ODD-MIGRATE-008 Close issue #74.
  - Route: parent after all evidence and explicit user authorization.
  - Evidence: every acceptance criterion is met, CI is green, and the issue closure comment links the cutover and verification evidence.

## Current evidence
- The React taxonomy surface, detail panels, browser-state store, static-export preparation, and G4/G6 supporting slices are merged into `develop`.
- FastAPI still serves `web/`; this branch has not changed that behavior.
- `src/modules/research/` remains the main functional gap.
- G6 canonical dry-run is recorded, but G3 Tier-2, G4 candidate parity, G5 candidate comparison, activation, and rollback rehearsal remain incomplete.

## Work-unit policy
No work-unit commit is made unless the user explicitly authorizes commits. Once authorized, each completed task must include its tests/docs and use a Conventional Commit message; the final atomic cutover is a separately authorized release boundary.
